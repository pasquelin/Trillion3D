# esbuild's install script only checks its native binary, which it finds in its platform package
# anyway; allowing it avoids pnpm's warning on each install. It is the only allowed dependency script.
allowBuilds:
  esbuild: true
