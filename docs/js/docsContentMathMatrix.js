/**
 * API reference for Matrix math functions.
 */
export const MATRIX_CONTENT = {
  multiplyMatrix4: {
    title: 'multiplyMatrix4(out, a, b)',
    type: 'Function',
    category: 'Matrices',
    signature: 'multiplyMatrix4(out: Float64Array, a: Float64Array, b: Float64Array): Float64Array',
    description: 'Computes out = a · b for 4×4 column-major matrices in double precision.',
    replaces: 'Matrix4.multiplyMatrices(a, b)',
    proof: 'Bench Matrix4.multiplyMatrices (1.2× faster, bit-identical)',
    params: [
      { name: 'out', type: 'Float64Array', desc: 'Target buffer.' },
      { name: 'a', type: 'Float64Array', desc: 'First matrix.' },
      { name: 'b', type: 'Float64Array', desc: 'Second matrix.' },
    ],
    example: 'multiplyMatrix4(worldMatrix, parentMatrix, localMatrix);',
  },
  invertMatrix4: {
    title: 'invertMatrix4(out, m)',
    type: 'Function',
    category: 'Matrices',
    signature: 'invertMatrix4(out: Float64Array, m: Float64Array): Float64Array',
    description: 'Computes matrix inverse via cofactors. Singular matrices write 16 zeros.',
    replaces: 'Matrix4.invert()',
    proof: 'Bench Matrix4.invert (1.3× faster, bit-identical)',
    params: [
      { name: 'out', type: 'Float64Array', desc: 'Target buffer.' },
      { name: 'm', type: 'Float64Array', desc: 'Source matrix.' },
    ],
    example: 'invertMatrix4(viewMatrix, cameraWorld);',
  },
  composeMatrix4: {
    title: 'composeMatrix4(out, position, quaternion, scale)',
    type: 'Function',
    category: 'Matrices',
    signature:
      'composeMatrix4(out: Float64Array, position: ArrayLike<number>, quaternion: ArrayLike<number>, scale: ArrayLike<number>): Float64Array',
    description: 'Builds affine transform out = Translation · Rotation · Scale.',
    replaces: 'Matrix4.compose(pos, rot, scale)',
    proof: 'Bench Matrix4.compose (1.4× faster)',
    params: [{ name: 'out', type: 'Float64Array', desc: 'Output matrix.' }],
    example: 'composeMatrix4(m, [0, 5, 0], [0, 0, 0, 1], [1, 1, 1]);',
  },
  decomposeMatrix4: {
    title: 'decomposeMatrix4(m, position, quaternion, scale)',
    type: 'Function',
    category: 'Matrices',
    signature:
      'decomposeMatrix4(m: Float64Array, position: Float64Array, quaternion: Float64Array, scale: Float64Array): void',
    description: 'Decomposes 4×4 matrix into translation, quaternion, and scale.',
    replaces: 'Matrix4.decompose()',
    proof: 'Bench Matrix4.decompose (1.1× faster)',
    params: [{ name: 'm', type: 'Float64Array', desc: 'Source matrix.' }],
    example: 'decomposeMatrix4(worldMatrix, pos, rot, scale);',
  },
  basisMatrix4: {
    title: 'basisMatrix4(out, u, v, n, origin, outAt = 0)',
    type: 'Function',
    category: 'Matrices',
    signature:
      'basisMatrix4(out: Float64Array, u: ArrayLike<number>, v: ArrayLike<number>, n: ArrayLike<number>, origin: ArrayLike<number>, outAt?: number): Float64Array',
    description: 'Constructs matrix from basis vectors (columns u, v, n) and origin.',
    replaces: 'Matrix4.makeBasis() + setPosition()',
    proof: 'Bench Matrix4.makeBasis (1.7× faster)',
    params: [{ name: 'out', type: 'Float64Array', desc: 'Target 16-element buffer.' }],
    example: 'basisMatrix4(out, right, up, forward, origin);',
  },
  uniformScaleMatrix4: {
    title: 'uniformScaleMatrix4(out, s, center, outAt = 0)',
    type: 'Function',
    category: 'Matrices',
    signature:
      'uniformScaleMatrix4(out: Float64Array, s: number, center: ArrayLike<number>, outAt?: number): Float64Array',
    description: 'Builds uniform scale matrix centered at 3D pivot coordinate.',
    replaces: 'Matrix4.makeScale() + setPosition()',
    proof: 'Bench Matrix4.makeScale (2.3× faster)',
    params: [{ name: 'out', type: 'Float64Array', desc: 'Output buffer.' }],
    example: 'uniformScaleMatrix4(m, 2.0, [0, 0, 0]);',
  },
  copyMatrix4: {
    title: 'copyMatrix4(out, m, outAt = 0, mAt = 0)',
    type: 'Function',
    category: 'Matrices',
    signature:
      'copyMatrix4(out: Float64Array, m: ArrayLike<number>, outAt?: number, mAt?: number): Float64Array',
    description: 'Copies 16 values between matrix buffers at given offsets.',
    replaces: 'Matrix4.copy(), fromArray(), toArray()',
    proof: 'Pure copy, bit equality verified',
    params: [{ name: 'out', type: 'Float64Array', desc: 'Destination.' }],
    example: 'copyMatrix4(dest, src, 0, 0);',
  },
  determinantMatrix4: {
    title: 'determinantMatrix4(m)',
    type: 'Function',
    category: 'Matrices',
    signature: 'determinantMatrix4(m: ArrayLike<number>): number',
    description: 'Computes the 4×4 scalar determinant of matrix m.',
    replaces: 'Matrix4.determinant()',
    proof: 'mathMatrix4.test.ts',
    params: [{ name: 'm', type: 'ArrayLike<number>', desc: '16-element matrix.' }],
    example: 'const det = determinantMatrix4(m);',
  },
  linearPartDeterminant: {
    title: 'linearPartDeterminant(m)',
    type: 'Function',
    category: 'Matrices',
    signature: 'linearPartDeterminant(m: ArrayLike<number>): number',
    description: 'Computes determinant of upper-left 3×3 sub-block to detect reflections.',
    replaces: 'Sub-determinant calculations',
    proof: 'mathMatrix4.test.ts',
    params: [{ name: 'm', type: 'ArrayLike<number>', desc: '16-element matrix.' }],
    example: 'const isReflected = linearPartDeterminant(m) < 0;',
  },
};
