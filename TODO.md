# Backlog

Une ligne = une tâche, et rien que ce qui reste à faire. Les règles de travail sont dans
[`AGENTS.md`](AGENTS.md), la cible du moteur dans
[`docs/SPEC_MOTEUR_SANS_THREE.md`](docs/SPEC_MOTEUR_SANS_THREE.md), l'historique, les mesures et les
décisions dans Git. Les numéros sont des noms : une tâche finie s'efface, les autres gardent le leur.

## Ordre de priorité

Décidé le 18 sept. 2026, sur les mesures de la campagne complète (`scripts/mesure/campagne.mjs`,
rapport `rapportGlobal.mjs`) : d'abord ce qui change le rendu et les images par seconde, ensuite ce
qui rend de la mémoire, les finitions en dernier.

0. **CRITIQUE, avant tout le reste** — Compilateur 0 (la scène `whisperwind-village` sort sans
   niveaux grossiers : 1 FPS, mémoire GPU débordée) et Mesure 2 (cette scène devient la seconde
   référence du banc). Tant que ce n'est pas fait, aucun chiffre du moteur ne vaut hors d'Emerald.
1. **Rendu et FPS** — Lumière 17 (le soleil coûte 4,6 ms sur 10 depuis la rue : cascades
   gardées d'une image à l'autre), Lumière 1 (image stable à 8 lampes), Géométrie 13 (une passe par
   matériau : le socle sans lampe vaut déjà un Three nu), Géométrie 9 (Hi-Z par visibilité passée),
   Géométrie 12 (ombres par la même géométrie), Lumière 2, 4, 6 ; Géométrie 27 (vues lointaines) ne
   s'ouvre que sur la mesure faite après Compilateur 0.
2. **Mémoire, donc petites machines** — Géométrie 11 (8,7 octets par triangle, comme Unreal),
   Textures T5 (compression à la cuisson), Géométrie 5 (une copie par grappe, l'instance en index),
   Textures T3, T2, T2ter, T4 (mesures), Géométrie 28 (ce qui reste des réservoirs de mémoire).
3. **Fidélité et matériaux** — Lumière 7, 10, 11, 12, 13 ; Compilateur 10 (masque tramé, verdict à
   l'œil) ; Géométrie 25 (campagne face à Unreal sur la même machine).
4. **Finitions, nettoyages, outillage** — tout le reste : Géométrie 1–3, 6, 7, 14–18, 21–24, 26 ;
   Lumière 3, 5, 8, 15 ; Compilateur 1, 2, 4–9, 11, 12 ; Calculateur 1–3 ; Mesure 0, 1.

## Géométrie

Le lag ne vient pas de la coupe (0,5 à 2,8 ms processeur par image sur 10 M de triangles, mesuré le
17 sept. 2026 sur `emerald-square`) : il vient de ce que notre moteur ACCEPTE et que la référence
REFUSE.

|                                   | La référence                        | Nous                        | Ligne                  |
| --------------------------------- | ----------------------------------- | --------------------------- | ---------------------- |
| Matériaux du chemin virtualisé    | opaque et masqué seulement          | on accepte aussi le mélange | Compilateur 10         |
| Grappes partagées entre instances | une fiche, l'instance en index      | douze copies                | Géométrie 5            |
| Sommets                           | quantifiés                          | bruts, ~48 octets/triangle  | Géométrie 11           |
| Textures                          | virtuelles, comprimées à la cuisson | tuiles RGBA brutes          | Textures T5            |

1. La carte garde et dessine la coupe, le processeur ne la connaît plus (décision du 17 sept. 2026 ; le coût processeur est de 0,5–2,8 ms par image, la référence ~0) : la liste dessinable quitte le relevé — le noyau `gpuDagCompactWgsl` part avec elle —, un gestionnaire de diffusion consomme les demandes classées (dernière image demandée pour l'éviction, priorité pour la file), et `webgpuCutDelta`, `webgpuCutCounts`, `webgpuCutPending`, `webgpuCutAdoption`, `webgpuCutPublication`, `webgpuHeldKeys`, `webgpuBudgetRanking`, `webgpuPinUpdater` partent alors. Fichiers `gpuDag*`, `webgpuCut*`.
2. Transparents en double face : dos et face posent deux pipelines par item — 6 016 appels au banc `transparents-ordres` contre 9 en simple face —, et porter le côté par instance sans élimination matérielle coûte +42 % (11,0 → 15,4 ms, apple metal-3) : une autre forme est à trouver. La transmission garde une tranche par entrée tant que le volume du matériau est un uniforme à décalage dynamique ; une primitive non paginée garde son appel tant que sa géométrie n'est pas concaténée. Fichiers `webgpuBlend*`.
3. Élagage par le haut : mesurer en millisecondes ce que rapporte la liste vivante rétrécie (seule sa taille l'est, −24 à −45 % de candidates selon la pose) ; et l'image où une primitive qui escalade retombe une image sur sa couverture épinglée avant de retrouver ses étages. Fichiers `gpuDag*`.
5. Une coupe par grappe, placements en index : 163 316 grappes × 12 placements = 1 959 792 fiches de 96 octets (`webgpuPagesLayout.ts:25`, `gpuDagPack.ts:153`) ; fiches uniques, placement en index dans la fiche de travail. Mémoire GPU ÷ 12 — à confronter au registre d'allocations, qui lit aujourd'hui 7,8 Mo de grappes + 8,5 de cônes + 41,8 de table (18 sept. 2026), avant de tenir le ÷ 12 pour acquis. Après 1–3 et 10.
6. Image tenue conservée à l'arrivée d'une page qui ne change rien à l'écran (`webgpuPagesPageApi.ts:37-44`).
7. Surveillance de scène par version : `hostSceneWatch` compare 4 300 nœuds × 18 valeurs par image (`frameGateCore.ts`).
9. Hi-Z : choisir les occulteurs par visibilité passée au lieu de la médiane de profondeur (557 352 lignes classées pour 21 955 testées), puis deux passes — dessiner le visible, bâtir la pyramide, re-tester les rejetés dans la même image.
10. Preuve de navigation sur la scène réelle : une trajectoire sur Emerald dont chaque pose est affirmée à l'image, hors banc bit à bit.
11. **Géométrie au poids d'Unreal : 8,7 octets par triangle en mémoire, mesuré, à la fin de ce seul lot** (aujourd'hui ~48 ; Three 36). Tout à la cuisson, dans le compilateur Rust, nouvelle version de format de page : positions quantifiées sur la grille de la grappe (14–16 bits/axe), normales octaédriques sur 2 octets, UV entiers, index sur quelques bits par triangle (base + décalages, la grappe n'a que 128 triangles), tangentes non stockées et recalculées, sommet partagé entre deux grappes écrit une fois. Le moteur décode sur la carte graphique. **Sans aucune perte de qualité au rendu** : la quantification se règle par grappe pour rester sous le pixel à la distance où la grappe est choisie, jamais au-dessus. Preuve : image identique au bit près, ou à 1 niveau près par canal expliqué, à qualité maximale sur les quatre vues du banc, captures avant/après publiées ; et le rapport publie les octets par triangle résidents — la ligne reste ouverte tant que le chiffre mesuré dépasse 8,7 ou que l'image bouge.
12. Ombres par la même géométrie : même sélection, même raster, mêmes pages depuis la lumière.
13. Matériaux par classes : une passe par matériau avec profondeur matérielle au lieu d'un branchement par pixel. À relire alors : au seuil 1 la sélection ne grossit pas ses grappes quand l'écran rétrécit (1 033 809 triangles à 1248×702 contre 1 099 276 à 2496×1404, soit ×3,8 par pixel).
14. Eau en passe plein écran dédiée (copie du fond déjà en place pour la transmission).
15. Noyaux Rust/Wasm M5 sur la coupe WebGL, la coupe de secours et la reconstruction des rangs.
16. Compilateur : `DAG_GROUP_MIN` (`dag.rs:22`) n'est lu que par les tests, `dag/build.rs` ne coupe que sur le plafond ; `lib.rs:102` garde la constante morte `CLUSTER_TRIANGLES = 256`.
17. `dagWanted` : un mot compact par grappe (nœud, drapeaux, monde) pour ne plus enregistrer les 80 % de grappes rejetées, à mesurer contre les 0,6 ms de la tête de sélection.
18. `flatHierarchy` (`gpuDagPack.ts`) laisse le plafond d'erreur du sous-arbre à −1 et la sphère du remplaçant à zéro : une primitive dont le manifeste ne porte pas de hiérarchie n'est élaguée ni par le haut ni par le bas. Les dériver des pages comme `cullingBounds` le fait déjà ; la fixture `gpuDagCutFrontierScene.ts` les remplit à la main, ce que le producteur devrait faire. Même preuve que le lot 3.
21. `orderBlendPasses` (`webgpuBlendOrder.ts`) refait quatre parcours complets des items par image — clés à l'œil, rejet par le tronc, puis les deux tris — même quand l'œil et les six plans sont ceux de l'image précédente. Sortir tôt sur l'empreinte de la vue, comme `keepMoved`/`orderMoved` le font pour les envois.
22. Coins de boîtes alloués par PLACEMENT : `createBoxCorners(packedPages.length)` (`hizCorners.ts:125`) prend 1 959 792 × 24 flottants doubles ≈ 376 Mo de RAM hôte, plus 7,8 Mo d'époques — le plus gros tampon processeur du moteur. Le lot 5 ne l'efface pas, les coins étant en monde ; les dériver par image des bornes locales et de la matrice, comme le noyau GPU le fait déjà.
23. Part des images d'une traversée réelle qui ont un transfert de texture en attente : `webgpuTexturePump.pump` sort tôt sur file vide, donc la note par clé de grappe (−30 %, 1,87 → 1,31 ms sur 20 000 placements) n'est payée qu'à cache froid. Non mesurée, et c'est elle qui dit ce que ce gain vaut.
24. Les numéros de liaison des groupes de calcul DAG et de l'étalement du mélange sont écrits 3 à 4 fois — WGSL, disposition, entrées, double des tests. Hi-Z et dessin le font déjà bien (`hizBindEntries`, `drawBindEntries`, publiés sous leur WGSL ; `webgpuBindLayout.ts` tient la règle pour les passes de rendu) : publier `DAG_BINDINGS` et `EXPAND_BINDINGS` de la même façon. Le côté qui dérive en silence est le double processeur : il lit le mauvais tampon sans rien lever.
25. Campagne face à la référence, sur LA MÊME MACHINE : même scène exportée des deux côtés, même pose, même résolution, les deux profils relevés ici. C'est la seule chose qui rendrait comparables les millisecondes du §3 de `docs/REFERENCE_UE5.md`, qui écrit « inconnu » plutôt qu'un écart supposé. Les chiffres restent hors dépôt, sous `.mesure/` : ils ne valent que sur le matériel qui les a pris.
26. Raster de calcul : éteint, le matériel dessine (18 sept. 2026 ; sur apple metal-3, rendu par tuiles, le partage petits/grands de la référence PERD — vue sol 5,2 → 7,7 ms, générale 7,8 → 19,8 à 1248×702, `.mesure/out/l26-4-hybride-quart-prod`). Ses deux variantes de diagnostic (`raster-calcul`, `raster-hybride`) restent dans le dépôt pour une seule chose : la mesure sur une carte de bureau, où le raster matériel paye un triangle d'un pixel au prix d'un quad — `--variante-apres raster-hybride`, 1248×702 et 2496×1404 sous budget. Il ne se rallume que sur cette mesure.
27. **Vues lointaines — la piste de la référence, sur mesure seulement** (décision du 18 sept. 2026). Déjà tenu : l'erreur d'écran par grappe (formule certifiée, plus stricte que la formule au centre de sphère), la racine de 128 triangles comme plancher d'un objet lointain (une visite par objet), le raster de calcul (ligne 26). Refusé, avec le motif : imposteurs/billboards (la référence les a livrés puis retirés du compilateur, plus compilés en 5.2), seuils fixes de diamètre écran 8/32 px (chiffres inventés), points/particules (aucun objet mobile chez nous), régulateur PID du seuil sur le temps GPU (la référence garde 1 px fixe et régule par la résolution dynamique ; ici la résolution ne baisse jamais et les budgets ne se lisent pas sur la machine), redessin du lointain une image sur K par zones d'écran reprojetées (au repos l'image est déjà tenue, en mouvement les bords de zones sont fragiles, et le coût visé est pris par (b) ou (d)). Ouvert, dans l'ordre : (a) après Compilateur 0, mesurer sur `whisperwind-village` la part de l'image des objets lointains — racines dessinées, triangles sous 4 px, ms de raster ; (b) si elle pèse, une **super-racine inter-instances au cook** — le compilateur groupe les placements voisins et bâtit une racine commune au-dessus des racines d'objet ; au loin une visite et 128 triangles pour dix objets ; le HLOD de la référence rangé dans le DAG, sans maillage à côté, mémoire ajoutée minuscule ; le prix : une étape de compilation qui connaît la scène (placements) là où tout est compilé objet par objet, et un format qui accepte un parent commun à plusieurs objets ; (c) à défaut, le HLOD tel quel — maillage fusionné par cellule, cuit, en grappes, pagé sous le même budget, qui duplique la géométrie là où Géométrie 5 la partage ; (d) sous raster de calcul rallumé (ligne 26), un **point pondéré à la place de la grappe** sous un diamètre projeté k px (mesuré, jamais choisi) : un point à la profondeur, couleur et normale moyennes cuites par grappe — version simple des voxels de la référence — ; change l'image sur ces pixels, `tri = selected` n'y tient plus, donc captures avant/après et écart déclaré ; (e) feuillage lointain en voxels dans le DAG (Nanite Foliage, 5.7, expérimental fin 2025) — à revoir quand la référence l'aura sorti d'expérimental.
28. **Réservoirs de mémoire, ce qui reste** (le lot Géométrie 0 est fusionné : pools de 512 Mio réglables par `setMemoryBudgets`, cibles d'image sans plafond, chiffres dans le message du commit et sous `.mesure/out/l-budget/`). (a) Sous ~2× la couverture racine — 4 Mio en vue générale sur Emerald, 2 730 fentes — la pose ne se tient plus et le témoin A/A monte à 119 858 px : l'hystérésis d'`admitGpuCut` (doubler, diviser sous 70 %) oscille entre deux coupes ; à regarder avant de vendre un curseur qui descend si bas. (b) Sous un pool de textures plus petit que la vue, QUELLES tuiles résident dépend de l'ordre d'arrivée des lectures : deux exécutions diffèrent de 14 000 à 46 000 px (canal max 50) sur `sol` et `rue` à 64 Mio — c'est le comportement de la référence, mais un curseur devrait l'afficher. (c) Les tables par page dessinable (`webgpuRowState`, coins, table de pages, Hi-Z) sont taillées une fois au plafond de session `geometryPoolCeilingBytes` ; les faire grandir au redimensionnement supprimerait le plafond et l'option. (d) Les groupes de liaison des passes de dessin sont invalidés par liste (`dropPoolBindGroups`) là où ceux des ombres le sont par identité de ressources : une seule règle, par identité.

## Lumière

1. Image stable d'abord : caméra mobile, 8 lampes + soleil donne 0 / 1 392 / 6 278 px sur 3 exécutions identiques et 14 erreurs de page par exécution. Isoler deux fois chaque cas — caméra fixe, soleil seul, lampes seules, ombres coupées, aucune lampe — et lire les erreurs. Fini quand 3 exécutions donnent 0 px.
2. Occulteurs d'ombre hors écran (`webgpuPagesEncodeShadowPass.ts`) : un mur derrière la caméra doit ombrer le sol visible.
3. Proxy lointain et rebond qui suivent les objets déplacés (`webgpuPagesTransform.ts`) : une porte déplacée déplace l'ombre lointaine et le rebond.
4. Toutes les lampes contributrices par tuile : retirer le plafond de 32 et donner une profondeur de tuile aux surfaces transparentes contre le ciel (`gpuLightTilesShader.ts`). 33 lampes, aucune perdue.
5. Test d'ombre du rebond pour chaque lampe (`bounceSurfaceWgsl.ts`) : aucune fuite à travers un mur avec 5 lampes.
6. Rejet des occulteurs dans le budget d'ombres (`stageMapping.ts`), puis saut du test derrière une surface, atlas à la demande, proxy seulement avec un soleil, profondeur conservée sur un simple changement de couleur. 0 px. À tenir aussi : à caméra mobile le budget d'1 ms laisse 192 pages d'ombre en attente deux images de suite et la cascade capturée dépend du moment — témoin A/A à 189 740 px en vue sol à 1248×702, 0 px dès `--ombres off` (18 sept. 2026) ; le banc attend la cascade complète pour la capture, ou le relevé dit que l'ombre est en retard.
7. Miroirs : repartir de develop, réutiliser la fixture `classes-materiaux/miroir.gltf` et les patches `.mesure/patches/lot-reflet/` (+ `lot-reflet.bundle`, base d48b66a) comme lecture, trouver pourquoi on/off = 0 px. Fini quand les cubes apparaissent dans le sol métallique.
8. Copies publiques de lampes détachées (`explorerLightApi.ts`) : muter une copie ne touche jamais l'interne.
10. Après 7 : réflexions rugueuses, ombres colorées semi-transparentes, translucidité du feuillage. Aucune conversion BLEND → MASK dans le moteur : la référence ne reclasse jamais d'elle-même, elle refuse le matériau et le nomme (sa géométrie virtualisée n'accepte que l'opaque et le masqué). Le travail est côté compilateur, et sur réponse explicite — Compilateur 10.
11. Rebond 3 (`lot/rebond-3`) : cascades de sondes autour de la caméra, budget en millisecondes plutôt qu'en nombres de maillages et de rayons, base d'harmoniques sphériques d'ordre 2, activé par défaut quand le budget tient.
12. Ombres des matériaux en mélange : une grappe transparente n'obtient pas de ligne de visibilité (`webgpuRowSync.ts`), n'entre jamais dans la table des rangs dessinés des cartes d'ombre et n'ombre rien. L'ombre atténuée et colorée d'une surface semi-transparente reste un lot à part.
13. Éclairage stochastique par pixel (RX2) : le rejet par tuile est livré, l'échantillonnage non. L'historique passe par les cibles en ping-pong de l'antialiasing temporel (`temporalAntialiasing.ts`, livré), pas par un tampon de stockage — c'est le même historique, monté une seule fois. Demande aussi un banc où les lampes atteignent vraiment les surfaces transparentes.
15. Couvrir `createDeferredLayouts` dans `webgpuBindBudget.test.ts`, et exécuter `prepareWebgpuPages` de bout en bout : rien ne le fait aujourd'hui.
17. **Le soleil coûte 4,6 ms sur une image de 10** (17 sept. 2026, Emerald vue sol à 2496×1404, caméra mobile : enveloppe 10,0 / 14,3 ms p50/p95 avec `--soleil`, 5,4 / 7,7 sans), autant que le tampon de visibilité et la passe matériaux réunis, et aucun lot ne le visait. À faire : (a) le coûter poste par poste — ombres, tuiles, éclairage avec ombres — par soustraction d'enveloppes (`--ombres off`, puis sans tuiles), jamais par lecture des passes ; (b) garder les cascades d'une image à l'autre — ne redessiner que les pages d'ombre dont la géométrie ou la lumière a changé, comme les cartes d'ombre virtuelles de la référence —, à caméra fixe d'abord (0 px), puis mobile.

## Textures

1. Verdict de l'utilisateur sur T1 (fusionné f43f6a44) : ce que la caméra regarde net d'abord — à montrer sur le banc, le Lab n'existant plus.
3. T2bis, reste : ce que le réseau coûte à une session qui ne lit que ce que la caméra regarde n'est pas mesuré — le banc draine tout à la barrière, 0,94 Go de niveaux contre 0,59 Go de PNG sources ; et l'admission mémoire du compilateur ignore les images (Compilateur 11).
4. T2 — traversée à cache froid : temps par image plafonné pour les transferts, report à l'image suivante, aperçu dessiné tant que le niveau manque ; regarder p95 et pic, pas la médiane. Depuis T2bis les niveaux intermédiaires existent : c'est maintenant mesurable.
5. T2ter — filtre de mips. La chaîne cuite reproduit la règle de la carte pour ne pas changer l'image (T2bis). L'ancien sidecar faisait mieux à deux titres, jetés à cette occasion : moyenne en linéaire PRÉMULTIPLIÉ, qui empêche la couleur d'un texel transparent de déteindre sur ses voisins opaques, et respect de la courbe de transfert déclarée par le fichier (une source déclarée linéaire était décodée deux fois). Les reprendre change l'image aux niveaux grossiers du feuillage — un lot à part, à juger à l'œil avec captures avant/après et écart publié, comme T5.
6. T3, reste à mesurer : dériver la demande de tuile de l'entrée que l'échantillon a déjà lue (zéro lecture de plus) ; loger la demande dans la surface `flags` plutôt que dans une cinquième cible (−4 octets par pixel).
7. T4 (df41abd4), mesures à faire : (a) relevés aux deux tailles (1248×702, 2496×1404) avec captures avant/après du feuillage ombré (`--avant` = arbre extrait du commit T0), coût à cache froid p95 et pic (T2), deux exécutions par point ; (b) vérifier que le témoin de tenue d'image tient à 2496×1404. Coût déclaré : 37 px à ±1 d'une demande de tuile qui vacille d'une exécution à l'autre ; l'ombre d'un caster hors caméra lit la tuile la plus fine que la trajectoire a laissée.
8. T5 — compression des blocs à la cuisson, **BC7 et ASTC ensemble** pour couvrir ordinateur et mobile : ~1 octet par texel contre 4, soit 6,69 → ~1,7 Go de niveaux ESTIMÉ sur Emerald, à relever au banc avant d'être affirmé. La pyramide est cuite (T2bis), le pool est en tuiles de 128² + gouttière de 4 (T4) : reste (1) un encodeur dans le compilateur — aucun n'existe, `texture2ddecoder` et `basisu` sont décodeurs seuls ; il se greffe au décodage unique de `texture_preview/bake.rs`, et la clé de cache suit (`build.rs`) ; (2) côté moteur, demander la capacité de compression à l'ouverture (`explorerCapabilities.ts` n'en demande aucune), tuiles et transferts en blocs de 4×4, repli RGBA8 ; (3) le registre d'octets (`gpuDeviceLedger.ts`) compte les formats comprimés. Risque principal, à mesurer tôt : le temps de compilation — 297 textures 2048² × pyramide × deux formats ; le disque du cache grossit, le réseau non. À livrer avec images avant/après et écart mesuré publié ; les seuils 0 px du banc ne s'appliquent pas à ce lot, ils restent entiers pour la géométrie et l'éclairage.

## Compilateur

0. **CRITIQUE — le simplificateur reçoit des sommets non soudés et ne réduit rien.** Mesuré le 18 sept. 2026 sur `whisperwind-village` (FBX d'Unreal, 400 Mo, 172 M de triangles instanciés, 10 717 nœuds) : chaque maillage sort de l'import à 3 sommets par triangle, aucun partagé (Emerald, en glTF, ~1). `weld_positions` (`dag/clusters.rs`) existe mais ne sert qu'aux verrous ; `reduce_group` (`dag/groups.rs`) passe à `simplify_with_locked_vertices` les indices d'origine, et meshoptimizer verrouille tout point qui existe en plus de deux exemplaires. Résultat : 70 primitives sur 101 en profondeur 0 (`noCollapse` 1 024 sur le maillage de 1,6 M), 83 % des triangles instanciés sans aucun niveau grossier, 65 601 racines sur 83 874 pages (Emerald : 2 668 sur 44 689), donc `INITIAL_COVERAGE_BUDGET: 65549 pages required` à 20 000 pages et le processus GPU tué à 100 000. Le compilateur a tout noté dans `clusters.json` et laissé passer en silence ; la référence soude à l'import et ne sort jamais un maillage sans racine. Preuve isolée (meshoptimizer JS, même simplificateur) : `Wall` 37 404 tri, brut → 37 404, soudé puis compacté → 18 702 ; idem sur `cimen`, `baca`, `S_Wooden_Bollard`, `SM_BlackAlder_Field_02` ; au-delà de ÷2 certains calent, cause à trouver. À faire : (a) souder par position — et compacter — avant de simplifier, en gardant les attributs de l'une des copies pour les niveaux grossiers ; (b) un DAG qui reste en profondeur 0 sur une primitive de plus d'un cluster est un AVERTISSEMENT nommé dans `clusters.json` et au journal, jamais un silence, et le moteur le remonte en diagnostic à l'ouverture ; (c) voir si `ufbx` rend les sommets dédupliqués dès l'import comme le glTF. Fini quand Whisperwind recompilé a toutes ses primitives de plus de 128 triangles en profondeur > 0, moins de 10 % de racines, et que le banc y rend une image (Mesure 2) ; Emerald identique à l'octet.
1. Réutiliser un produit compilé complet quand chaque empreinte de dépendance et le produit existent déjà, au lieu de reconstruire le DAG. Sortie identique à l'octet, comme les tâches 2 à 5.
2. Coupe linéaire pour les n-gones convexes dans `ngon.rs` (quadratique aujourd'hui, 8 000 coins = 113 ms) ; les faces concaves gardent la coupe en oreilles.
4. Extraire un unitypackage en une passe, en gardant CRC, bornes, annulation et sans publication partielle.
5. Admission avant import, et points d'annulation dans les longues coupes de n-gones et le calcul des normales.
6. Garder les blocs DDS/KTX2 sur le GPU sans les décoder.
7. Accepter en entrée le glTF compressé Draco et meshopt.
8. Map industrielle sur le banc 15.
9. Vérification de licence FAB pour le corpus.
10. Découpe, reste : le masque TRAMÉ par-dessus — la référence ne masque pas en dur, elle trame le masque et son antialiasing temporel (livré) remélange ces points en couverture partielle, si bien qu'une feuille à 30 % reste à 30 % au lieu de disparaître ; sans lui, le coût mesuré est une perte de matière (6,31 % des pixels en moyenne sur dix poses de la marche d'Emerald, 36,4 % au pire, couronnes éclaircies, pelouse en grains). Puis le verdict à l'œil de l'utilisateur, captures avant/après ; et les appels de dessin et FPS sur le banc en regardant la pelouse, contre 271 appels et 8 FPS avant le lot.
11. Admission mémoire du compilateur : les images n'y entrent pas (`plan_buffers`, `compiler_build.rs:63`) alors que la cuisson de la pyramide tourne sur la grappe du travail — un 4096² décodé fait 67 Mo, plus 16 Mo de champ de distance. Borner le parallélisme par la crête et refaire passer l'estimation.
12. Une texture déjà tranchée est remesurée à chaque compilation : 0,87 s des 0,93 s sur `emerald-square` une fois les quinze réponses données, indéfiniment. La feuille porte déjà la mesure par empreinte d'image ; la resservir demande d'y graver aussi une empreinte des seuils de `cutout/measure.rs`, sinon un seuil déplacé rendrait une mesure périmée sans que rien ne le dise.

## Calculateur

1. Sur go de l'utilisateur : notre propre moteur de rendu WebGL2, puis l'API hôte sans types Three. `PageRec.matrix` et `ClusterRoot.world` sont déjà des matrices remplies par le moteur (`hostWorldPlacements.ts`) ; les aplatir en `Float64Array` ne demande rien à l'hôte, seuls ses lecteurs — `addInstance`/`updateInstance` et les moteurs témoins — prennent encore un `THREE.Matrix4`. Compté le 18 sept. 2026 : 147 fichiers du moteur navigateur importent `three` hors tests — 124 en créent des objets (`Matrix4`, `Vector3`, caméras, maillages), 23 n'en prennent que les types —, 53 lisent `.elements`, 122 fichiers de test en dépendent aussi ; sdk-core n'en a qu'un. Par lots, chacun prouvé à l'image identique : matrices et vecteurs (nos maths existent déjà), puis caméra, puis maillages témoins et API hôte.
2. Rafraîchissement ciblé d'un nœud déplacé : `setWebgpuTransform` recalcule tout l'index moteur (`hostWorldPlacements.refresh`) là où `updateWorldMatrix(true, true)` ne parcourait que les ancêtres et le sous-arbre — hors chemin d'image, mais O(scène) par `setTransform`. `updateNodeWorldMatrix` (`packages/sdk-core/mathTransformTreeUpdate.ts`) suffit.
3. À mesurer quand c'est commode : gain des pages en mémoire partagée (`--isolation on` vs off) et du chemin Wasm (`--chemin-math js` vs `wasm`) ; `normalMatrix3` par fragment dans `visibilityShadingNormal.ts` ; listes de lampes de `gpuLightTilesShader` à ciel ouvert.

## Mesure

0. `partition-gpu-conservatrice` : le cache de référence recompilé par notre compilateur ne porte que des `exact-clusters` (281 primitives, aucune `clustered-blend`) — les matériaux mélangés d'Emerald sont reclassés à l'import. La moitié opaque de la preuve tourne ; sa moitié transparente se déclare « non examinée » tant que le cache ne porte aucune grappe transparente. Pour la rejouer : une scène des assets avec un vrai matériau mélangé.
1. Le Lab est supprimé, aucune liaison ne doit rester : remonter la preuve Emerald (`test/browser/emeraude-webgpu.browser.mjs`) sur le serveur du harnais — scène de `.mesure/assets/`, poses de `poses.mjs`, témoin Three de `pageTemoin.mjs` —, retirer `beaute-webgpu` et `presentation-gpu` (montées sur ses pages, code mort sans lui), `LAB_URL` et `adresseDuLab` (`test/appui/browserFixtureServer.mjs`, `test/test-gpu.mjs`), et les mentions de `docs/SDK.md`, `docs/TESTS.md`, `AGENTS.md`.
2. **`whisperwind-village` devient la seconde scène de référence du banc**, à côté d'Emerald : FBX d'Unreal, soupe de sommets, instanciation lourde (un mur × 1 292 = 48 M de triangles), 81 matériaux dont 6 texturés — l'export n'a pas sorti les textures Megascans, ce n'est pas nous, à dire dans le rapport et à ré-exporter côté Unreal. Copiée le 18 sept. 2026 sous `.mesure/assets/whisperwind-village{,-derived}/` (hors git). Le 18 sept. 2026, la mesure est impossible : `--max-pages 100000` tue le processus GPU de Chrome (`exit_code=9`) avant la première image, 20 000 refuse par `INITIAL_COVERAGE_BUDGET`. À faire, après Compilateur 0 : trouver pourquoi 100 000 pages débordent ici et pas sur Emerald (le lot budget mémoire en Mio, non commité, est concerné) ; la campagne (`campagne.mjs`, `rapportGlobal.mjs`) joue les deux scènes, même relevé, même rapport ; le témoin Three LOD aussi, puisqu'il a été vu « bien meilleur » au Lab sans qu'on sache pourquoi. Fini quand le rapport HTML a une colonne Whisperwind avec ses quatre barres et un verdict.
