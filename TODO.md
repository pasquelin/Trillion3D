# Backlog

Une ligne = une tâche. Les règles de travail sont dans [`AGENTS.md`](AGENTS.md), la cible du moteur
dans [`docs/SPEC_MOTEUR_SANS_THREE.md`](docs/SPEC_MOTEUR_SANS_THREE.md), l'historique et les
décisions dans Git.

## Ordre de priorité

Décidé le 18 sept. 2026, sur les mesures de la campagne complète (`scripts/mesure/campagne.mjs`,
rapport `rapportGlobal.mjs`) : d'abord ce qui change le rendu et les images par seconde, ensuite ce
qui rend de la mémoire, les finitions en dernier.

1. **Rendu et FPS** — Géométrie 0 (budget d'image selon la machine, sinon la 4K et le raster de
   calcul restent refusés), Lumière 17 (le soleil coûte 4,7 ms sur 10,7 depuis la rue : cascades
   gardées d'une image à l'autre), Lumière 1 (image stable à 8 lampes), Géométrie 13 (une passe par
   matériau : le socle sans lampe vaut déjà un Three nu), Géométrie 9 (Hi-Z par visibilité passée),
   Géométrie 12 (ombres par la même géométrie), Lumière 2, 4, 6.
2. **Mémoire, donc petites machines** — Géométrie 11 (8,7 octets par triangle, comme Unreal),
   Textures T5 puis T4 (compression à la cuisson, pool fixe), Géométrie 5 (une copie par grappe,
   l'instance en index), Textures T3, T2, T2ter, et le refus `ALL_PAGES_PINNED` sous budget serré
   (dégrader, jamais s'arrêter — à rattacher à Géométrie 0).
3. **Fidélité et matériaux** — Lumière 7, 10, 11, 12, 13, 14 ; Compilateur 10 (verdict à l'œil) ;
   Géométrie 25 (campagne face à Unreal sur la même machine).
4. **Finitions, nettoyages, outillage** — tout le reste : Géométrie 6, 7, 14–18, 21–24 ; Lumière 3,
   5, 8, 9, 15 ; Compilateur 1, 2, 4–9, 12 ; Calculateur 1–3 ; Mesure 0, 1.

## Géométrie

**Priorité, mesurée le 17 sept. 2026 sur `emerald-square` et non plus supposée** : le lag ne vient pas
de la coupe. Le processeur y tient en 0,5 à 2,8 ms par image sur 10 millions de triangles, coupe,
relevé et résidence compris. Il vient de ce que notre moteur ACCEPTE et que la référence REFUSE.

C'est l'écart à combler, et il n'est pas dans les algorithmes — le DAG de grappes, la sélection par
la carte, le raster de calcul, le Hi-Z en deux passes et les demandes de diffusion priorisées sont
livrés et alignés. Il est dans les CONTRAINTES :

|                                   | La référence                        | Nous                        | Ligne                  |
| --------------------------------- | ----------------------------------- | --------------------------- | ---------------------- |
| Matériaux du chemin virtualisé    | opaque et masqué seulement          | on accepte aussi le mélange | Compilateur 10         |
| Grappes partagées entre instances | une fiche, l'instance en index      | douze copies                | Géométrie 5            |
| Sommets                           | quantifiés                          | bruts, ~48 octets/triangle  | Géométrie 11           |
| Textures                          | virtuelles, comprimées à la cuisson | atlas entier, RGBA brut     | Textures T2bis, T4, T5 |

Ce que « iso » veut dire, ce sont les quatre critères de parité d'`AGENTS.md`, et aucun n'est tenu :
budgets mémoire et millisecondes fixes (non : 7,56 Go de textures brutes, grappes dupliquées douze
fois, sommets non compressés) ; résidence pilotée par ce que l'image lit vraiment (vrai pour la
géométrie, faux pour les textures — et hors d'atteinte tant que la pyramide a un trou entre 64 px et la
pleine résolution, Textures T2bis puis T3) ; compression à la cuisson (non : ni Textures T5 ni
Géométrie 11) ; aucun travail dans une scène immobile (l'image tenue existe, mais Géométrie 7 et 19
restent). Les quatre sont des critères de MÉMOIRE autant que de vitesse.

L'ordre de travail est en tête du fichier (« Ordre de priorité »).

0. **CRITIQUE — le budget d'image est un chiffre en dur.** `DEFAULT_FRAME_BUDGET` (`webgpuPagesSetup.ts:19`, 288 Mio) est un relevé fait une fois sur ce Mac, arrondi : la même limite pour toute machine. Une grosse carte est bridée (la 4K et le raster de calcul à 2496×1404 sont refusés par `SURFACE_BUDGET: 415 Mo > 288 Mio`, mesuré le 18 sept. 2026, alors que la machine les tiendrait), une petite plante au lieu de se restreindre. À faire : le budget se calcule à l'ouverture d'après ce que l'appareil déclare — `maxBufferSize`, taille max de texture, `navigator.deviceMemory` —, 288 Mio en plancher pour que l'image de référence passe partout, un plafond, et repli d'un cran si une allocation échoue ; `maxFrameAllocationBytes` reste la main de l'hôte. Fini quand, sur ce Mac, `raster-2496` de `campagne.mjs` est admis, et quand un budget forcé à 288 Mio rend l'image de référence identique au bit près à aujourd'hui.
1. **Fait, moitié processeur** : coupe publiée comme une différence par les deux chemins sous un contrat unique, lecteurs tenus sur l'entrant/sortant ; `webgpuBudgetRanking`, `gpuDagRuntime` et `webgpuFrameHold` fondus dedans. **La moitié GPU ne vise plus la différence mais la RÉFÉRENCE** (décision du 17 sept. 2026) : le processeur doit cesser de connaître la coupe, la carte la garde et la dessine — le raster de calcul lit déjà son masque sur place —, et le relevé ne rapporte qu'une liste de demandes de diffusion de taille fixe. Appartenance persistante, journal séparé et protocole de resynchronisation tombent avec ce choix : il n'y a plus rien à resynchroniser. **Fait** : relevé borné par `SELECTION_LIST_CAP`, débordement dit et replié sur la coupe processeur ; totaux de triangles tenus par `dagMask` ; chaque rang est une demande portant sa priorité, classée à la relecture ; oracle au même contrat, prouvé sur la carte ; le classement du relevé est un TRI PAR COMPTAGE sur les 1 024 pas de priorité — deux parcours, aucune comparaison, aucun tampon intermédiaire, et stable, donc substituable au tri par rangs qu'il remplace : 40,0 → 0,42 ms au plafond de 262 144 rangs, 4,70 → 0,05 ms à 31 000. **Reste** : la liste dessinable doit quitter le relevé (le noyau de compaction `gpuDagCompactWgsl` disparaît avec elle), un gestionnaire de diffusion doit consommer les demandes — dernière image demandée pour l'éviction, priorité pour la file —, et `webgpuCutDelta`, `webgpuCutCounts`, `webgpuCutPending`, `webgpuCutAdoption`, `webgpuHeldKeys`, `webgpuBudgetRanking`, `webgpuPinUpdater` partent alors. Fichiers `gpuDag*`, `webgpuCut*`.
2. **Fait, scène simple face** : les transparents paginés d'un même pipeline tiennent en une tranche, le plan trié est lu par la carte seule, l'argument indirect par item a disparu — 3 008 appels → 9 au banc `transparents-ordres`. **Rien n'est gagné en double face** (6 016 → 6 016) : dos et face posent deux pipelines par item, aucune tranche ne fusionne ; porter le côté par instance sous un pipeline sans élimination matérielle coûte +42 % de passe transparente (11,0 → 15,4 ms, apple metal-3), refusé tant qu'une autre forme n'est pas trouvée. **Reste** : la transmission garde une tranche par entrée tant que le volume du matériau est un uniforme à décalage dynamique (le rapatrier porte `VSOut` à seize variables inter-étages, le plancher garanti) ; une primitive non paginée garde son appel tant que sa géométrie n'est pas concaténée. Fichiers `webgpuBlend*`.
3. **Fait** : la descente entière tient dans une passe, chaque niveau lancé à plat sur la taille de son étage, six commandes par image au lieu de 3·profondeur+3 — 0,26 → 0,15 ms à profondeur 5, un niveau valant ~26 µs ouvert derrière ses copies contre ~1,4 µs à plat (banc `coupe-lancements-gpu`, mêmes pages au bit près). **L'élagage par le haut est livré** : le nœud porte le plancher d'erreur du sous-arbre et la sphère qui l'englobe (16 → 24 flottants), que `cullingBounds` dérive des pages à la préparation — rien du compilateur, rien du format —, et `errorFloor` le projette en WGSL. Mesuré sur apple metal-3 (`elagage-haut-gpu`, coupe identique au bit près dans les quatre poses) : candidates et vivantes retirées de 24,3 % de face, 24,5 % de biais, 45,2 % de loin, 99,1 % au contact — cette dernière sur une coupe vide, tout y étant trop grossier. Justesse : `plancher-erreur-cpu-gpu` compare coupe processeur, oracle et noyau WGSL sur la carte, `gpuDagCutFloor.test.ts` interdit qu'une grappe retenue disparaisse, et `coupe-lancements-gpu` retrouve les mêmes pages et les mêmes dessinées que la descente d'avant. **Persistance refusée** : la frontière doit être réexaminée à chaque image, ce qu'une coupe gardée économiserait plafonne à 0,47 % (`gpuDagCutFrontier.test.ts`). **Fils persistants refusés** : WGSL ne garantit aucun progrès entre groupes de travail. **Reste** : le gain en millisecondes de la liste vivante rétrécie n'est pas mesuré, seule sa taille l'est ; et à l'image où une primitive se met à escalader, son seuil franchit un plancher élagué et elle retombe une image sur sa couverture épinglée — l'image suivante élague au seuil escaladé et retrouve ses étages. Fichiers `gpuDag*`.
4. **Fait, la note (a)** : ce qui ne dépend pas de la caméra sort de la boucle d'image — sphère de la boîte, couches du matériau, étendue uv —, bâti à la première vue d'une grappe et tenu par CLÉ DE GRAPPE, douze placements partageant une ligne comme au lot 5 ; la pose est retenue d'une page à la suivante, la coupe étant groupée par primitive. 1,87 → 1,31 ms par image sur 20 000 placements, −30 % aux trois tailles (banc `pompe-textures`, verdict identique au bit près : niveau voulu et poids de chaque couche, couleur et données). **File par insertion refusée** : le tri ne pèse rien — 0,005 ms à 64 travaux, 0,14 ms à 2 000, contre 2,4 ms pour la note à 20 000 pages. **Reste** : la note n'est payée que lorsqu'un transfert attend (`webgpuTexturePump.pump` sort tôt sur file vide), donc à cache froid et non en régime établi ; `Math.hypot` → `sqrt` vaudrait encore ~0,35 ms sur 20 000 pages mais change les derniers bits du rayon, non pris ; et (b) textures virtuelles — 1 pixel sur 16 incrémente un compteur par tuile dans la passe de résolution, lu par `mapAsync` une image en retard, texture d'indirection pour l'atlas ; en WebGL2 par une petite image relue —, à mener avec Textures T3 et T4, c'est le même mécanisme. Fichiers `texturePriority*`.
5. Une coupe par grappe, placements en index : 163 316 grappes × 12 placements = 1 959 792 fiches de 96 octets (`webgpuPagesLayout.ts:25`, `gpuDagPack.ts:153`) ; fiches uniques, placement en index dans la fiche de travail. Mémoire GPU ÷ 12. Après 1–4 et 10.
6. Image tenue conservée à l'arrivée d'une page qui ne change rien à l'écran (`webgpuPagesPageApi.ts:37-44`).
7. Surveillance de scène par version : `hostSceneWatch` compare 4 300 nœuds × 18 valeurs par image (`frameGateCore.ts:125`).
8. **Fait** : chaque rang du relevé est une demande portant sa priorité — l'erreur d'écran du remplaçant, quantifiée sur dix bits par pas logarithmique de 2^(1/16) —, et la relecture rend la liste classée. Même grandeur que le chemin WebGL2 (`orderPendingUrls`), prouvée contre `clusterErrorPixels` du socle sur une scène à quatre profondeurs, et contre l'oracle sur la carte (`plancher-erreur-cpu-gpu`, mêmes priorités dans cinq poses). Fichiers `gpuDagRequest*`.
9. Hi-Z : choisir les occulteurs par visibilité passée au lieu de la médiane de profondeur (557 352 lignes classées pour 21 955 testées), puis deux passes — dessiner le visible, bâtir la pyramide, re-tester les rejetés dans la même image.
10. **Fait, les tests** : les six `webgpuPages` rouges depuis b72278c6 affirmaient la géométrie opaque dessinée par `drawIndirect`, que ce commit avait déplacée sur le raster de calcul ; Géométrie 26 a remis le matériel en production le 18 sept. 2026, et `webgpuPages.03` et `.09` affirment de nouveau les commandes indirectes, le masque de l'image en plus. Les autres nommés ont été passés à la mutation : `gpuDagLive`, `webgpuBindEntries`, `frameCostAudit` et `webgpuPages.11` mordent et restent tels quels ; trois gardes ne mordaient pas et sont prouvées (`webgpuRowRecycle.test.ts` pour l'alias de ligne, `gpuDagSelection3.test.ts` pour la moitié monde du relevé en vol, la copie non due et la résidence republiée). **Reste** : preuve de navigation sur la scène réelle. Le banc bit à bit est fait, Géométrie 26. Le tableau référence / nous est livré — `docs/REFERENCE_UE5.md`, gardé par `test/integration/reference-ue5.test.mjs`, qui relit les constantes dans le code et refuse que le document dérive ; ce qu’il laisse ouvert est la campagne de la ligne 25.
11. **Géométrie au poids d'Unreal : 8,7 octets par triangle en mémoire, mesuré, à la fin de ce seul lot** (aujourd'hui ~48 ; Three 36). Tout à la cuisson, dans le compilateur Rust, nouvelle version de format de page : positions quantifiées sur la grille de la grappe (14–16 bits/axe), normales octaédriques sur 2 octets, UV entiers, index sur quelques bits par triangle (base + décalages, la grappe n'a que 128 triangles), tangentes non stockées et recalculées, sommet partagé entre deux grappes écrit une fois. Le moteur décode sur la carte graphique. **Sans aucune perte de qualité au rendu** : la quantification se règle par grappe pour rester sous le pixel à la distance où la grappe est choisie, jamais au-dessus. Preuve : image identique au bit près, ou à 1 niveau près par canal expliqué, à qualité maximale sur les quatre vues du banc, captures avant/après publiées ; et le rapport publie les octets par triangle résidents — la ligne reste ouverte tant que le chiffre mesuré dépasse 8,7 ou que l'image bouge.
12. Ombres par la même géométrie : même sélection, même raster, mêmes pages depuis la lumière.
13. Matériaux par classes : une passe par matériau avec profondeur matérielle au lieu d'un branchement par pixel.
14. Eau en passe plein écran dédiée (copie du fond déjà en place pour la transmission).
15. Noyaux Rust/Wasm M5 sur la coupe WebGL, la coupe de secours et la reconstruction des rangs.
16. Compilateur : `dag/groups.rs:20` n'applique pas `DAG_GROUP_MIN` ; `lib.rs:101` constante morte `CLUSTER_TRIANGLES = 256`.
17. `dagWanted` : un mot compact par grappe (nœud, drapeaux, monde) pour ne plus enregistrer les 80 % de grappes rejetées, à mesurer contre les 0,6 ms de la tête de sélection.
18. `flatHierarchy` laisse le plafond d'erreur du sous-arbre à -1 et la sphère du remplaçant à zéro : une primitive dont le manifeste ne porte pas de hiérarchie n'est donc élaguée ni par le haut ni par le bas, et perd le levier mesuré à 24-45 % sur les candidates. Les dériver des pages comme `cullingBounds` le fait déjà ; la fixture `gpuDagCutFrontierScene.ts` les remplit aujourd'hui pour pouvoir mesurer, ce que le producteur devrait faire. Demande la même preuve que le lot 3.
19. **Fait** : le relevé n'est plus taillé sur le pire cas. Il valait `16 + pageCount*4` par moitié — une coupe qui retiendrait le catalogue entier — et la copie d'image en emportait la totalité : 15,2 Mo par image à 1 992 187 grappes, plus 30,4 Mo de fentes relisibles. Borné à 262 144 rangs : 2,0 Mo copiés, 4,0 Mo de fentes. Le TEMPS, lui, n'est pas affirmé — sur apple metal-3, mémoire unifiée, l'écart entre les deux tailles est du même ordre que la bande de bruit de la carte (`test/justesse/releve-coupe-gpu.mjs` le publie avec sa bande). Rétrécir le relevé rend de la mémoire ; les millisecondes se gagneront en le supprimant.
20. **Fait** : les totaux de triangles sont tenus par `dagMask` et voyagent dans l'entête du relevé, au lieu d'être sommés par le processeur sur la différence de coupe. Les trois sont pris sur le même ensemble — la coupe dessinable entière —, ce qui tient `selected − drawn − uncovered = 0` par construction ; un premier jet comptait la coupe retenue et vérifiait donc l'invariant par accident. Coût : un mot par grappe au froid, un bit de mélange au chaud, entête de quatre à huit mots. **L'ADOPTION LES LIT** depuis le 17 sept. 2026 : `webgpuCutAdoption` prend ceux de l'entête et ne retombe sur la somme processeur que lorsqu'il n'y en a pas — c'est-à-dire sur le seul repli `adoptCpuCut`. La somme de `webgpuCutCounts` cesse donc d'être payée par le chemin carte, et part avec le reste des lecteurs du §1. La carte, elle, ne dispute plus quatre mots uniques par grappe vivante : chaque groupe de 64 fils somme dans sa mémoire partagée puis verse un sous-total, soit soixante-quatre fois moins de disputes pour les mêmes nombres — `dagMask` franchit ses barrières sur tous ses fils, y compris ceux qui n'ont pas de grappe. Prouvé sur carte : `ecartOracleGpu` nul et mêmes totaux dans les cinq poses de `plancher-erreur-cpu-gpu`.
21. `orderBlendPasses` refait quatre parcours complets des items par image — clés à l'œil, rejet par le tronc, puis les deux tris — même quand l'œil et les six plans sont ceux de l'image précédente. Sortir tôt sur l'empreinte de la vue, comme `keepMoved`/`orderMoved` le font déjà pour les envois.
22. Coins de boîtes alloués par PLACEMENT : `createBoxCorners(packedPages.length)` prend 1 959 792 × 24 flottants doubles ≈ 376 Mo de RAM hôte, plus 7,8 Mo d'époques (`webgpuPagesLayout.ts`, `hizCorners.ts:125-127`) — le plus gros tampon processeur du moteur. Le lot 5 ne l'efface pas, les coins étant en monde ; les dériver par image des bornes locales et de la matrice, comme le noyau GPU le fait déjà.
23. Part des images d'une traversée réelle qui ont un transfert de texture en attente : `webgpuTexturePump.pump` sort tôt sur file vide, donc le gain du lot 4a n'est payé que là. Non mesurée, et c'est elle qui dit ce que ce lot vaut.
24. Les numéros de liaison des groupes de calcul DAG et étalement du mélange sont encore écrits 3 à 4 fois — WGSL, disposition, entrées, double des tests. Hi-Z et dessin sont faits (`hizBindEntries`, `drawBindEntries`, publiés sous leur WGSL) ; `webgpuBindLayout.ts` tient déjà la règle pour les passes de rendu. Reste à publier `DAG_BINDINGS` et `EXPAND_BINDINGS` de la même façon. Le côté qui dérive en silence est le double processeur : il lit le mauvais tampon sans rien lever.
25. Campagne face à la référence, sur LA MÊME MACHINE : même scène exportée des deux côtés, même pose, même résolution, les deux profils relevés ici. C’est la seule chose qui rendrait comparables les millisecondes du §3 de `docs/REFERENCE_UE5.md` — aujourd’hui leur profil est pris sur la console de la démo et le nôtre sur un portable, et le document écrit « inconnu » plutôt qu’un écart supposé. Les chiffres relevés restent hors dépôt, sous `.mesure/` : ils ne valent que sur le matériel qui les a pris.
26. **Fait, et clos sur cette machine : le raster de calcul est éteint, le matériel dessine, et le tri de la référence ne fait rien gagner.** (1) Le calcul ne se crée plus que sous une variante de diagnostic, jamais par le seul budget de mémoire (a51cdc57) ; en rallumant le matériel, `hiz-rig-camera` a montré qu'il perdait toute grappe sortant de derrière une autre depuis 12aa9fcd — la troncature de la moitié testée lisait « verdict nul » sur un verdict à trois valeurs —, corrigé, et le verdict a maintenant un contrat (`VERDICT_*`, `hizRejected` partagé, 8f5b3bef). Mesuré le 18 sept. 2026 (`.mesure/out/l26-1-quart`, `l26-1-pleine`) : à 1248×702 vue sol **25,2 → 5,7 ms**, vue générale **73,3 → 7,7** ; à 2496×1404 même chemin des deux côtés, 0 px. (3) Le banc bit à bit a chiffré les défauts du calcul — une frise de 1 011 px sur un pare-chocs coupé par le bord, des fissures pointillées sur les arêtes partagées — et leur cause : la couverture se décidait sur des poids dérivés ; réécrite sur les trois arêtes en ordre canonique, poids normalisés sur leur somme (f2f14839, `l26-3-quart-final` : générale 135 → 45 px, sol 2 796 → 2 020, le reste silhouettes et départages à profondeur égale ; preuve `raster-calcul-etanche`, 0 px). (2) Le partage petits/grands de la référence est écrit — `COMPUTE_TAKES_WGSL`, le même prédicat dans les deux rasters, les résolutions du calcul fondues sous `greater-equal` dans ce que le matériel a posé — sous la variante `raster-hybride`, 0 px sur la preuve, 2 et 71 px sur Emerald. Et il PERD : à 1248×702 (`l26-4-hybride-quart-prod`, soleil, antialiasing) vue sol **5,2 → 7,7 ms**, vue générale **7,8 → 19,8** ; sans ombres ni antialiasing 4,9 → 6,6 et 4,4 → 17,4. La passe matérielle passe de 1,04 à 1,11 ms quand on lui retire les petits triangles — elle ne les sentait pas — et le calcul dépense 1,5 à 6 ms pour les mêmes. À 2496×1404 ses tampons dépassent le budget de parité (`SURFACE_BUDGET 414 880 528 > 301 989 888`). C'est une mesure d'UNE carte, apple metal-3, rendu par tuiles ; la référence tire son gain des cartes de bureau où le raster matériel paye les triangles d'un pixel au prix d'un quad. Décision du 18 sept. 2026 : le raster de calcul et ses deux variantes restent dans le dépôt, éteints, prêts à être re-mesurés sur une carte de bureau — `--variante-apres raster-hybride` sur le banc, à 1248×702 et à 2496×1404 sous budget. Sans une telle mesure, il ne s'allume pas, et la ligne ne rouvre pas.

## Lumière

1. Image stable d'abord : caméra mobile, 8 lampes + soleil donne 0 / 1 392 / 6 278 px sur 3 exécutions identiques et 14 erreurs de page par exécution. Isoler deux fois chaque cas — caméra fixe, soleil seul, lampes seules, ombres coupées, aucune lampe — et lire les erreurs. Fini quand 3 exécutions donnent 0 px.
2. Occulteurs d'ombre hors écran (`webgpuPagesEncodeShadowPass.ts`) : un mur derrière la caméra doit ombrer le sol visible.
3. Proxy lointain et rebond qui suivent les objets déplacés (`webgpuPagesTransform.ts`) : une porte déplacée déplace l'ombre lointaine et le rebond.
4. Toutes les lampes contributrices par tuile : retirer le plafond de 32 et donner une profondeur de tuile aux surfaces transparentes contre le ciel (`gpuLightTilesShader.ts`). 33 lampes, aucune perdue.
5. Test d'ombre du rebond pour chaque lampe (`bounceSurfaceWgsl.ts`) : aucune fuite à travers un mur avec 5 lampes.
6. Rejet des occulteurs dans le budget d'ombres (`stageMapping.ts`), puis saut du test derrière une surface, atlas à la demande, proxy seulement avec un soleil, profondeur conservée sur un simple changement de couleur. 0 px. À tenir aussi : à caméra mobile le budget d'1 ms laisse 192 pages d'ombre en attente deux images de suite, et la cascade capturée dépend du moment — témoin A/A à 189 740 px en vue sol à 1248×702, 0 px dès `--ombres off` (`.mesure/out/l26-1-quart`, `l26-1-quart-sans-ombres`, 18 sept. 2026) ; le banc doit pouvoir attendre la cascade complète pour la capture, ou le relevé doit dire que l'ombre est en retard.
7. Miroirs : repartir de develop, réutiliser la fixture `classes-materiaux/miroir.gltf` et les patches `.mesure/patches/lot-reflet/` (+ `lot-reflet.bundle`, base d48b66a) comme lecture, trouver pourquoi on/off = 0 px. Fini quand les cubes apparaissent dans le sol métallique.
8. Copies publiques de lampes détachées (`explorerLightApi.ts`) : muter une copie ne touche jamais l'interne.
9. Coût GPU de la vue au sol, 3,65 → 6,49 ms à 0 px : bissection, sur machine calme seulement.
10. Après 7 : réflexions rugueuses, ombres colorées semi-transparentes, translucidité du feuillage. Aucune conversion BLEND → MASK dans le moteur : la référence ne reclasse jamais d'elle-même, elle refuse le matériau et le nomme (sa géométrie virtualisée n'accepte que l'opaque et le masqué). Le travail est côté compilateur, et sur réponse explicite — Compilateur 10.
11. Rebond 3 (`lot/rebond-3`) : cascades de sondes autour de la caméra, budget en millisecondes plutôt qu'en nombres de maillages et de rayons, base d'harmoniques sphériques d'ordre 2, activé par défaut quand le budget tient.
12. Ombres des matériaux en mélange : une grappe transparente n'obtient pas de ligne de visibilité (`webgpuRowSync.ts`), n'entre jamais dans la table des rangs dessinés des cartes d'ombre et n'ombre rien. L'ombre atténuée et colorée d'une surface semi-transparente reste un lot à part.
13. Éclairage stochastique par pixel (RX2) : le rejet par tuile est livré, l'échantillonnage non. L'historique passe par les cibles en ping-pong de l'antialiasing temporel (`temporalAntialiasing.ts`, livré), pas par un tampon de stockage — c'est le même historique, monté une seule fois. Demande aussi un banc où les lampes atteignent vraiment les surfaces transparentes.

14. Vue sans lumière : reprendre le correctif b57ebd89 (« unlit WebGL publie l'albédo brut, métal compris »), aujourd'hui commit non référencé — `git branch hold/unlit-albedo-three b57ebd89` avant qu'il soit ramassé. La référence affiche la couleur de base telle quelle, métaux compris ; notre noir vient de l'ambiant π appliqué sur le PBR, où le diffus d'un métal est nul par construction. Pas de capacité « non fidèle sur les métaux » à déclarer.
15. Couvrir `createDeferredLayouts` dans `webgpuBindBudget.test.ts`, et exécuter `prepareWebgpuPages` de bout en bout : rien ne le fait aujourd'hui.

16. **Fait** : l'antialiasing temporel de la référence, à résolution native — gigue Halton (2,3) sur la seule matrice de rendu (la sélection, ses plans et son seuil ne la voient pas ; la partition projette avec la matrice qui a rasterisé sa pyramide), vecteurs de mouvement DÉRIVÉS du tampon de visibilité et de la pose précédente du placement (`taaMotion.ts`, identité pour l'immobile, ancrés sur l'œil), deux cibles d'historique en ping-pong, composition sur la cible écrite, images tenues exclues. Deux régimes : un huitième en mouvement ; à l'image calme, l'historique repart en phase zéro et la k-ième pèse 1/k, si bien que l'image tenue est la moyenne uniforme de seize images de l'état final — **0 px A/A** mesuré sur Emerald avec l'option, là où l'exponentielle gardait 12 % de l'ordre d'arrivée des textures (24 % des pixels, max 61). PROUVÉ (`test/browser/antialiasing-temporel.browser.mjs`) : seuls les deux pixels d'un bord changent, intérieur à 0 px, témoin A/A au bit près, aucun fantôme après déplacement, historique gardé sous panoramique (128 bords intermédiaires contre 82 s'il était rejeté). MESURÉ le 18 sept. 2026 (`.mesure/out/l16-*`, 2496×1404, soleil, caméra mobile, deux séries par côté) : enveloppe GPU p50 en vue sol 10,57 → 11,09 ms (+0,5, quatre relevés par côté entre 10,22–11,01 et 10,95–11,29 : de l'ordre de la bande), en vue générale 9,08 → 9,48 ms (+0,4, bandes 8,96–9,30 et 9,26–9,92 : au-dessus) ; p95 +1,5 et +1,4 ms — le prix d'une passe plein écran qui lit dix texels et en écrit un à 3,5 Mpx, poids du filtre et lectures de mouvement sortis du pixel ; image convergée à caméra fixe : 27,1 % des pixels de la vue sol changent (852 926 d'un niveau, 42 de plus de 64), 12,2 % de la vue générale. Deux choses que la TODO supposait et que la mesure a contredites : les cibles d'historique ne tenaient PAS dans le budget de 256 Mio à la résolution de relevé (275,7 Mo), le défaut passe à 288 Mio avec sa dérivation dans `webgpuPagesSetup.ts` ; et la durée de la passe par son étiquette ne vaut rien sur apple metal-3 (elle lit ~7,5 ms, tout le groupe d'éclairage), seule la différence d'enveloppe compte. **Reste** : pas de sur-échantillonnage (la référence remonte en 4K, nous non) ; l'accumulation ne couvre pas la capture de surfaces ni les vues de diagnostic, par contrat ; à l'arrêt, les bords se raidissent une image ou deux avant de reconverger — le prix du 0 px A/A, déclaré.
17. **Le soleil coûte 4,6 ms sur une image de 10, et les durées par passe mentent sous chevauchement.** MESURÉ le 17 sept. 2026 par `scripts/mesure/banc.mjs`, moteur seul, Chrome sans fenêtre, Emerald vue sol à 2496×1404, caméra mobile, 41 relevés `timestamp-query` par série. Avec `--soleil` : enveloppe de l'image **10,049 / 14,303 ms** p50/p95, et par passe `WG HDR composition + present` 7,075, `WG deferred lighting` 7,010, `WG light tiles v1` 2,500 avec UNE lampe, `WG shadow atlas v1` 2,164. Sans `--soleil` : enveloppe **5,397 / 7,718 ms**, composition 2,414, éclairage différé 2,379, tuiles et ombres absentes. Deux faits. (1) **Le soleil — ombres, tuiles, éclairage avec ombres — vaut 4,6 ms d'enveloppe**, autant que tout le tampon de visibilité et la passe matériaux réunis (2,186 + 2,372) ; c'est le plus gros poste de l'image, et aucun lot ne le visait. (2) **La composition ne coûtait pas 7 ms** : une passe qui ne dépend pas du soleil est tombée de 7,1 à 2,4 en le retirant, donc son horodatage de fin absorbait le travail des passes d'à côté — sur apple metal-3 les passes se chevauchent et la durée d'une passe ne lui appartient pas. RÈGLE tirée de là, à tenir dans tout rapport : l'enveloppe est le total, une passe dit _où_ mais jamais _combien_, et une différence entre deux séries ne se lit que sur l'enveloppe. **Fait, le relevé (b)** : `--variante presentation-hors-ecran` le 18 sept. 2026 (`.mesure/out/l17-hors-ecran`) — `WG HDR composition` sans `+ present` lit 6,83 / 7,14 ms p50 sur deux séries, contre 7,35 avec, et l'enveloppe 10,25 / 10,51 contre 10,72 / 11,01 : la présentation n'attend pas, l'étiquette absorbe le groupe d'éclairage qui la précède. **Reste** : coûter le soleil poste par poste par soustraction d'enveloppes (`--ombres off`, puis sans tuiles), jamais par lecture des passes. Ce lot donne ce que Lumière 9 cherchait par bissection.
18. **Fait, par Géométrie 26 : à un quart des pixels l'image était 2,6 fois plus lente parce que le raster de calcul s'y allumait, pas à cause de la passe matériaux.** Les 24 ms de `WG material surfaces v1` à 1248×702 (17 sept. 2026, `.mesure/out/l17-quart/`, `l17-quart-2/`) étaient l'étiquette qui absorbait le raster de calcul (0,997 ms au matériel, même taille) ; la densité de triangles par pixel n'était pas la variable, et la courbe de résolutions n'a plus d'objet. Depuis le lot 26, vue sol à 1248×702 : 5,7 ms contre 11,0 à 2496×1404. Ce qui reste de vrai et sans lot : au seuil d'erreur 1 la sélection ne grossit pas ses grappes quand l'écran rétrécit (1 033 809 triangles contre 1 099 276), donc ×3,8 triangles par pixel — à relire si une passe matériaux par matériau (Géométrie 13) est un jour mesurée.

## Textures

1. Verdict de l'utilisateur sur T1 (fusionné f43f6a44) : route `/?test=15-virtualized-integration`, ce que la caméra regarde net d'abord.
2. **T0 — voir avant d'agir.** Le moteur publie déjà SEIZE compteurs textures (`webgpuPagesMetrics.ts:59-80`, contrat `sdk-core/textureMetricsContracts.ts`, clés `backendMetricKeys.ts:38-53`) et aucun n'est affiché : le panneau du banc 15 (`render-tech-lab/src/components/ModelMetricsBody.tsx`) n'a qu'une ligne de mémoire, celle de la géométrie. Les 7,56 Go sont un relevé à la main gelé dans un commentaire (`textureBudget.test.ts:6-12`), pas une commande qu'on relance. **Fait, l'affichage** : le panneau du banc 15 lit maintenant les seize compteurs textures (`ModelTextureMetrics.tsx`) ET les durées carte graphique par passe et par bloc (`ModelGpuMetrics.tsx`, blocs de `gpuPassBlocks.ts` : tampon de visibilité, passe matériaux, le reste — une passe inconnue tombe dans « le reste », jamais dans un bloc comparé). **Fait, le relevé** : `scripts/mesure/banc.mjs` savait déjà tout mesurer par étape au `timestamp-query` ; lancé le 17 sept. 2026 à 2496×1404 (`--largeur 2496 --hauteur 1404 --soleil --camera-mobile`), vue sol : image entière 10,3 ms p50 / 13,8 p95 côté carte, processeur 0,40 / 0,50 ms, atlas 6 688 572 304 octets calculés — le chiffre du calcul hors carte, retrouvé sur l'appareil. **Reste** : l'étape « Géométrie » du banc (3,5 ms p50) confond raster et surfaces matériaux, que les blocs de `gpuPassBlocks.ts` séparent ; le banc doit conserver `gpuPassMs` dans `mesure.json` pour publier cette séparation. Les cinq bancs `.perf.mjs` textures ne mesurent que des millisecondes ; les octets viennent du relevé ci-dessus. **Fait** : `atlasClasses` passe à 2 par défaut (`explorerBackends.ts:85`, `webgpuPagesPrepareTextures.ts:79`), soit 872 359 272 octets rendus (−11,5 %) pour 15 142 pixels sur dix captures (0,012 %, amplitude max 159) — les deux chiffres étaient déjà publiés (`docs/SDK.md:314`), seule la décision manquait ; palliatif jusqu'à T4, qui l'efface. **Preuve faite le 17 sept. 2026 par le harnais du moteur**, `banc.mjs --avant HEAD`, vues sol et générale à 2496×1404 avec soleil : atlas **7,561 → 6,689 Go** lus sur l'appareil, image **0 px, max canal 0** sur les deux vues, témoin A/A 0 px (`.mesure/out/atlas-avant-apres/resume.md`) ; le coût publié de 15 142 px sur dix captures reste le coût déclaré, ces deux vues n'en montrent aucun. Le résumé du banc porte désormais une ligne Textures (`rapportTextures.mjs`) : les octets d'atlas ne sont plus un relevé à la main. **Reste de ce point** : `beaute-webgpu.browser.mjs`, seuil dur de 2 par canal sur dix-huit fixtures de matériaux — la seule preuve qui demande le serveur du Lab. `AGENTS.md:18` interdit d'optimiser un chemin dont le coût n'est pas mesuré : c'est ce lot qui lève l'interdit pour les six autres.
3. **T2bis — Fait : le compilateur cuit la pyramide, le moteur la streame.** Le trou était réel : le sidecar s'arrêtait à 64 px et la pleine résolution écrivait le niveau 0, donc pour une texture 2048² les niveaux 1 à 4 n'existaient nulle part. **Livré** : chaque texture d'atlas — couleur ET données, 672 entrées sur Emerald contre 336 couleur seule — a sa chaîne entière, la queue ≤ 64 px dans le sidecar (version 5, douze mots par entrée : atlas et niveaux cuits en plus), le reste en un PNG sans perte par niveau sous `native/textures/v3/<sha>/<srgb|linear>-<k>.png`, adressé par l'empreinte de l'image, jamais réécrit, purgé avec les objets ; le moteur (`textureSource: 'cache'`) ne décode plus une image source, lit les niveaux du grossier au fin dans l'ordre que l'écran dicte (`webgpuAtlasBakedJobs.ts`, `webgpuTextureReads.ts`, six niveaux lus tenus en mémoire au plus, aucun avant la première caméra), les deux atlas portent une résidence et la régénération GPU des mips ne sert plus qu'au chemin de repli. La règle de réduction est celle de la carte (`reduce.rs`, moyenne linéaire, alpha médian, niveau k depuis le k−1 quantifié), pas celle de l'ancien sidecar (prémultiplié, alpha remis à l'échelle) : cuire ne change pas l'image. MESURÉ le 18 sept. 2026, `banc.mjs --avant HEAD --cache-avant <cache v4> --textures cache`, Emerald 2496×1404, soleil, caméra mobile : **préparation 1,84 → 1,16 s** (le navigateur ne décode plus 565 Mo de PNG) ; enveloppe carte **inchangée à la dispersion près** — sans soleil 5,92 → 5,53 ms sur la pelouse, avec soleil 10,0–10,5 → 10,2–11,0 sur trois exécutions ; mémoire carte 6,69 Go inchangée, sans perte oblige ; compilation 14,5 → 10 s une fois les niveaux cuits, 20 s la première fois (13 s en PNG rapide, pour 1,0 Go au lieu de 856 Mo). **Image** : vue générale 0 px au-delà de ±2 par canal ; vue sol 1 720 pixels isolés (0,05 %) qui basculent au seuil de découpe — la médiane d'alpha d'un texel grossier tombe de l'autre côté de 0,5 selon l'arrondi flottant de la carte, que rien ici ne peut reproduire à l'unité près — et, soleil allumé, leurs ombres (4 244 px de 3 à 32). **Découvert en mesurant** : les cartes d'ombre n'étaient JAMAIS périmées par l'arrivée d'un niveau de texture — deux exécutions identiques rendaient deux ombres selon le moment où chaque page avait été dessinée (témoin A/A 379 476 px). Corrigé : un niveau couleur qui arrive périme toutes les pages, et `flush()` les redessine (huit images au plus, seulement si un niveau est arrivé). Ce correctif vaut aussi pour l'ancien chemin. **Reste** : le banc draine TOUT à la barrière — 0,94 Go de niveaux téléchargés contre 0,59 Go de PNG sources —, ce que le réseau coûte à une session qui ne lit que ce que la caméra regarde n'est pas mesuré ; le Lab n'a pas encore `textureSource: 'cache'` ; et l'admission mémoire du compilateur ignore toujours les images (Compilateur 11, seconde moitié).
4. T2 — traversée à cache froid : temps par image plafonné pour les transferts, report à l'image suivante, aperçu dessiné tant que le niveau manque ; regarder p95 et pic, pas la médiane. Depuis T2bis les niveaux intermédiaires existent : c'est maintenant mesurable.
5. **T2ter — filtre de mips.** La chaîne cuite reproduit la règle de la carte pour ne pas changer l'image (T2bis). L'ancien sidecar faisait mieux à deux titres, jetés à cette occasion : moyenne en linéaire PRÉMULTIPLIÉ, qui empêche la couleur d'un texel transparent de déteindre sur ses voisins opaques, et respect de la courbe de transfert déclarée par le fichier (constat 56 : une source déclarée linéaire était décodée deux fois). Les reprendre change l'image aux niveaux grossiers du feuillage — un lot à part, à juger à l'œil avec captures avant/après et écart publié, comme T5.
6. T3 — priorité par lecture de l'image rendue au lieu de l'estimation par taille à l'écran : 1 pixel sur 16 incrémente un compteur par tuile demandée dans la passe de résolution, lu par `mapAsync` une image en retard. C'est le retour d'image de la référence, et le même mécanisme que le lot Géométrie 4b : un seul chemin pour les deux.
7. T4 — tuiles de taille fixe dans un pool physique à budget constant, seules les tuiles vues résidentes, texture d'indirection pour dire où est chaque tuile (ou quel niveau grossier prendre en attendant). Remplace l'atlas dimensionné sur la plus grande texture et les classes de taille (`atlasClasses` 2 n'est qu'un palliatif). **Absorbe T1c** : la vraie libération n'est pas un lot à part, c'est une propriété du pool — aujourd'hui l'atlas est alloué d'avance et aucun octet n'est jamais rendu, `textureBudget.ts:12-15` et `:20-26` le disent en toutes lettres. C'est le seul des quatre critères de parité que la compression ne donne pas : la mémoire cesse de dépendre de la scène.
8. T5 — compression des blocs à la cuisson, **BC7 et ASTC ensemble** pour couvrir ordinateur et mobile, tuiles comprises : ~1 octet par texel contre 4, soit 7,56 → ~1,9 Go ESTIMÉ sur Emerald, à relever au banc de T0 avant d'être affirmé. **Impose T2bis** : le moteur fabrique ses mips par une PASSE DE RENDU (`textureMips.ts`, médiane d'alpha, atlas créé en `RENDER_ATTACHMENT`), et on ne rend pas dans une texture compressée — les mips doivent donc être cuits, compression et pyramide sont un seul lot. Deux conséquences à ne pas découvrir en route : la file de transfert passe de la ligne à la rangée de quatre lignes (`webgpuAtlasJobs.ts:writeRows`), et `BYTES_PER_TEXEL = 4` (`webgpuAtlasClasses.ts:21`) est supposé dans tout le registre d'octets. Côté compilateur, aucun encodeur n'existe — `texture2ddecoder` et `basisu` sont décodeurs seuls ; l'encodage se greffe au décodage unique de `texture_preview.rs:174-179`, comme la mesure d'alpha du lot 10, et la clé de cache suit seule (`build.rs:22-43`). Côté moteur, aucune capacité de compression n'est demandée (`explorerCapabilities.ts:99-112` n'en demande que deux) et il faut un repli RGBA8. Risque principal, à mesurer tôt : le temps de compilation — 297 textures 2048² × pyramide complète × deux formats, inconnu et probablement dominant ; le disque du cache grossit, le réseau non, un client ne prenant qu'un format et que les niveaux qu'il regarde. À livrer avec les images avant/après et l'écart mesuré publié ; les seuils 0 px du banc ne s'appliquent pas à ce lot, ils restent entiers pour la géométrie et l'éclairage.

## Compilateur

1. Réutiliser un produit compilé complet quand chaque empreinte de dépendance et le produit existent déjà, au lieu de reconstruire le DAG. Sortie identique à l'octet, comme les tâches 2 à 5.
2. Coupe linéaire pour les n-gones convexes dans `ngon.rs` (quadratique aujourd'hui, 8 000 coins = 113 ms) ; les faces concaves gardent la coupe en oreilles.
3. **Fait avec Textures T2bis** : une image est décodée une fois quelles que soient les textures et les atlas qui la lisent (`texture_preview/bake.rs`, groupement par image), et plus aucune pyramide ne dépend d'un seuil MASK — la règle de la carte n'en a pas.
4. Extraire un unitypackage en une passe, en gardant CRC, bornes, annulation et sans publication partielle.
5. Admission avant import, et points d'annulation dans les longues coupes de n-gones et le calcul des normales.
6. Garder les blocs DDS/KTX2 sur le GPU sans les décoder.
7. Accepter en entrée le glTF compressé Draco et meshopt.
8. Map industrielle sur le banc 15.
9. Vérification de licence FAB pour le corpus.
10. **Fait, le mécanisme ; reste le verdict à l'œil.** Le compilateur ne devine JAMAIS en silence — la référence, elle, refuse le matériau translucide, le nomme au journal, et attend qu'un humain coche « masqué ». Il MESURE donc l'alpha de chaque texture candidate dans le décodage que les aperçus font déjà (`texture_preview.rs`), PROPOSE un verdict, et n'applique que ce qu'une feuille de réponses lui dit. **Livré** : `cutout/measure.rs` (part de texels intermédiaires, part de ceux-là collés au contour du seuil à 8 px près, six tests en éprouvette) ; `decoupes.json` et `decoupes.html` écrits à CHAQUE compilation à la racine du modèle compilé, que quelque chose attende ou non — c'est la feuille qui dit si une relecture est due —, la page étant un fichier autonome, vignettes couleur et alpha incluses, sans serveur ni réseau ; réponses rangées par empreinte d'image, donc resservies par toute scène qui partage la texture ; refus nommés pour la transmission et le facteur alpha partiel ; ce qui change vraiment entre dans la clé du cache. MESURÉ sur `emerald-square` le 17 sept. 2026 (modèle d'essai, rien dans le code ne s'y rattache) : 16 textures candidates sous 29 liaisons, 15 proposées en découpe ; la seizième (`053.png`) n'a que 54 % de ses gris au contour contre 86 à 100 % pour les autres, et reste proposée en mélange. Réponses appliquées : **29 primitives en `clustered-blend` → 1**, 252 → 280 en grappes exactes, c'est-à-dire tout le feuillage passé sur le raster de calcul en un appel ; feuille remise à zéro, la scène est revenue à 252/29 au bit près. Coût de compilation : 0,93 s de mesure et 0,08 s de lecture des images candidates sur 14,5 s. **Reste** : le verdict à l'œil et les captures avant/après avec l'écart en pixels publié, comme `AGENTS.md` l'exige — masquer durcit la silhouette, et l'antialiasing temporel qui rattrape ce bord chez la référence est livré (Lumière 16) ; et la mesure des appels de dessin et des FPS dans le Lab, en regardant la pelouse, contre les 271 appels et 8 FPS relevés avant le lot.
11. **Fait pour moitié avec Textures T2bis** — l'étape tourne sur la grappe du travail, une image par ouvrier, 0,93 s → 0,66 s de mesure d'alpha et 10 s de compilation au lieu de 14,5. **Reste** l'admission mémoire : les images n'y entrent toujours pas. Le texte d'origine : les aperçus de texture et la mesure d'alpha des découpes forment la dernière boucle SÉQUENTIELLE du compilateur, alors que le pool rayon du travail est déjà bâti et inutilisé à ce moment : 0,93 s de mesure sur `emerald-square`, qui tomberait vers 0,15 s à huit fils, le décodage avec. Le frein est la crête mémoire — un 4096² décodé fait 67 Mo, plus 16 Mo de champ de distance —, donc borner le parallélisme et refaire passer l'estimation d'admission fait partie du lot. **Devient un prérequis de Textures T2bis**, et non plus un confort : cuire la pyramide complète de 297 textures 2048² passe par cette boucle, et les images n'entrent toujours pas dans l'admission mémoire (`plan_buffers`, `compiler_build.rs:63`).
12. Une texture déjà tranchée est remesurée à chaque compilation : 0,87 s des 0,93 s sur `emerald-square` une fois les quinze réponses données, indéfiniment. La feuille porte déjà la mesure par empreinte d'image ; la resservir demande d'y graver aussi une empreinte des seuils de `cutout/measure.rs`, sinon un seuil déplacé rendrait une mesure périmée sans que rien ne le dise.

## Calculateur

1. Sur go de l'utilisateur : notre propre moteur de rendu WebGL2, puis l'API hôte sans types Three. `PageRec.matrix` et `ClusterRoot.world` sont déjà des matrices remplies par le moteur (`hostWorldPlacements.ts`) ; les aplatir en `Float64Array` ne demande rien à l'hôte, seuls ses lecteurs — `addInstance`/`updateInstance` et les moteurs témoins — prennent encore un `THREE.Matrix4`. Une trentaine de fichiers lisent `.elements`.
2. Rafraîchissement ciblé d'un nœud déplacé : `setWebgpuTransform` recalcule tout l'index moteur (`hostWorldPlacements.refresh`) là où `updateWorldMatrix(true, true)` ne parcourait que les ancêtres et le sous-arbre — hors chemin d'image, mais O(scène) par `setTransform`. `updateNodeWorldMatrix` (`packages/sdk-core/mathTransformTreeUpdate.ts`) suffit.
3. À mesurer quand c'est commode : gain des pages en mémoire partagée (`--isolation on` vs off) et du chemin Wasm (`--chemin-math js` vs `wasm`) ; `normalMatrix3` par fragment dans `visibilityShadingNormal.ts` ; listes de lampes de `gpuLightTilesShader` à ciel ouvert.

## Mesure

0. `partition-gpu-conservatrice` : le cache de référence recompilé par notre compilateur ne porte que des `exact-clusters` (281 primitives, aucune `clustered-blend`) — les matériaux mélangés d'Emerald sont reclassés à l'import. La moitié opaque de la preuve tourne ; sa moitié transparente se déclare « non examinée » tant que le cache ne porte aucune grappe transparente. Pour la rejouer : une scène des assets avec un vrai matériau mélangé.
1. Le Lab est supprimé, aucune liaison ne doit rester : remonter la preuve Emerald (`test/browser/emeraude-webgpu.browser.mjs`) sur le serveur du harnais — scène de `.mesure/assets/`, poses de `poses.mjs`, témoin Three de `pageTemoin.mjs` —, retirer `beaute-webgpu` et `presentation-gpu` (montées sur ses pages, code mort sans lui), `LAB_URL` et `adresseDuLab` (`test/appui/browserFixtureServer.mjs`, `test/test-gpu.mjs`), et les mentions de `docs/SDK.md`, `docs/TESTS.md`, `AGENTS.md`.
