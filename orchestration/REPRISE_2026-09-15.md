# Prompt de reprise — 15 sept. 2026, fin de session (limite de compte)

Colle ce fichier tel quel dans la nouvelle session. Les agents de la session précédente sont morts avec elle : leurs identifiants ne servent plus, leur travail est sur disque (branches, worktrees, journal).

## Rôles et règles (rappel ferme)

Fable = chef : ne code pas, ne lit pas de code, décide et brief. Opus 5 = code. Sonnet 5 = tests, revues, mesures. Aucun agent sans « go » explicite de l'utilisateur, sauf enchaînement déjà autorisé. Réponses de 5 à 10 lignes. Règles du dépôt : AGENTS.md. Jamais de git stash, jamais de commit direct sur develop (branche + fusion). Lab render-tech-lab et public/benchmark-assets en lecture seule sauf ordre explicite. Port 5174 = serveur de l'utilisateur. Mots interdits dans le dépôt : Nanite, Unreal. Licence Xcode non acceptée : `DEVELOPER_DIR=/Library/Developer/CommandLineTools` et `/Library/Developer/CommandLineTools/usr/bin/git` ; l'utilisateur doit lancer `sudo xcodebuild -license`. Zéro perte sur les textures (règle utilisateur) ; poids minimal (charge du chef). Formats : orchestration/SPEC_FORMATS_IMPORT.md ; pas de format propriétaire hors lecture légale ; `.uasset`/`.umap` retirés.

## État de develop (4030cb8)

Fusionnés : chantier textures progressives lots 1 à 4 + simplify + revue ; correctif priorité pompe (5305f6c) ; loop-code P0 purge de cache (8bb630c) et P1 fixture dorée aperçus (861af9c) ; profil loop-code (hors git, `.agents/`, restauré sur disque) ; Village au banc 15 (Lab d44c995, journal 83568d9) ; documents formats et pilotes ; `test-assets/` ignoré.

## Branches vivantes, non fusionnées (worktrees sous .claude/worktrees/)

1. `fbx-opacite` (753345c + fusion develop 47c7eaf), worktree `agent-a852655bfe807bf43` : opacité des matériaux FBX Phong lue à l'import, rivière du Village en BLEND, 161 tests Rust. Une revue Sonnet était en cours : à relancer (rejouer rouge/vert, validate, fusionner). Suite : fusionner la carte d'opacité séparée dans l'alpha de baseColorTexture à l'import, sans perte.
2. `compilateur/routeur-plugins` (3047093, 5fc6201), worktree `agent-ae8efcf602081d64d` : contrats `scene-plugin-1` et `image-plugin-1`, pilotes gltf/fbx/obj/png/jpeg, PLUGINS.md, 165 tests Rust. Un Sonnet tests+revue+fusion était en cours : à relancer (8 comportements listés dans l'entrée de journal de la branche et dans le résumé : annulation, cache par fingerprint, ordre png/jpg/jpeg, rapports texture-*, plafond d'allocation, GLB sans extension, dossier FBX+OBJ ambigu, source inconnue). Conflit attendu avec `fbx-opacite` sur `IMPORTER_VERSION` : la version vit dans les pilotes (fbx/obj → `…-gltf-3`).
3. `mesure-sans-perte`, worktree `mesure-sans-perte` : mesure WebP/QOI/PNG optimisé/JXL sans perte, doublons, mémoire GPU par classes ; l'encodage WebP tournait (50/293 textures) ; le rapport `orchestration/mesures/textures-sans-perte-2026-09-15.{md,json}` n'est peut-être pas écrit. Relancer un Sonnet de mesure si absent.

## À faire ensuite, dans l'ordre

1. Relancer les deux revues (opacité, routeur), fusionner, supprimer worktrees et branches.
2. Vague 1 de pilotes, trois Opus en parallèle, un worktree chacun, brief = PLUGINS.md + SPEC_FORMATS_IMPORT.md : `tga`, `unity` (scène YAML, données seulement, LOD le plus fin, .mat → PBR), `zip`. Puis vague 2 : `tiff`, `dds` (blocs gardés sur GPU si acceptés), `unitypackage`. Puis le reste de la liste.
3. Industrial Map (/Users/pasquelin/Desktop/AI/map/Industrial Map, projet Unity, 36 FBX, 33 TGA) au banc 15 après tga + unity.
4. Défaut « aperçus 16×16 introuvables après import FBX » (7/7 écartés sur le Village, image-missing : les aperçus résolvent l'URI sous le glTF importé alors que les PNG restent au dossier source) : un lot Opus.
5. loop-code : P1 perf (décodage textures hors rayon ; copie `bytes.to_vec()` évitable) exigent une mesure 5+5 sur machine calme ; la mesure partielle (develop 2× plus lent que 9a7821a sur l'étape hiérarchie/pages, 2 exécutions, charge 55) est à refaire quand l'utilisateur dit que la machine est libre. P2 : deux passages pixel MASK.
6. Poids sans perte : selon la mesure, WebP sans perte sur le fil ; dédoublonnage ; plus de classes d'atlas ; puis virtualisation par tuiles sans perte (7,56 Go d'atlas calculés, mémoire dominante).
7. Décisions utilisateur en attente : option `atlasClasses: 2` (−872 Mo calculés, 15 142 px hors bruit sur objets lointains) ; départ au sol du banc 15 (règle sonde le point le plus bas, Lab verrouillé) ; licence des assets FAB (Village, Industrial) avant démonstration publique.
8. Jeu de modèles de test : l'utilisateur fait livrer par ChatGPT dans `/Users/pasquelin/Applications/webGeometry/test-assets/` (ignoré par git) avec INDEX.md et telecharger.sh ; les agents par format pointent dessus.

## Autres

- Rapport 15 du Lab : colonne WebGPU plus claire et plate que three-webgl-reference ; testé, ce n'est pas les textures (sRGB rejeté, flush = témoin à 3 px) ; à chercher côté éclairage/ombrage (autre chantier). 4 pixels de develop scintillent depuis les lots éclairage/ombres.
- Worktree de la session précédente `rust-mesh-dominant-color-046701` (branche `claude/rust-mesh-dominant-color-046701`) : à supprimer.
- Mémoire persistante : ~/.claude/projects/-Users-pasquelin-Applications-webGeometry/memory/ (fichiers webgeometry-textures-apercus-lots.md, webgeometry-compilateur-pilotes.md, reponses-cinq-lignes.md, agents-modeles-et-jetons.md).
