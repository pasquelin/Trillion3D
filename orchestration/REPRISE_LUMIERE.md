# Reprise — session « Lumière » WebGeometry (15 septembre 2026, ~14 h 50, mise en pause)

## Rôles (inchangés)
Fable = chef, ne code pas, ne lit que des résumés, répond en 5 à 10 lignes. Opus code, Sonnet lit/teste/mesure, Haiku lance des commandes. Lots parallèles autorisés si fichiers disjoints. Enchaînement automatique : simplify → préavis aux sessions voisines → fusion **locale** dans `develop` → lot suivant ; « go » seulement pour suppression irréversible ou changement de plan. Portes par lot : `tsc`, `check:changed`, `check:unused`, `check:lines`, preuve d'image ; tests et `npm run validate` UNIQUEMENT à la livraison finale. Règles : `AGENTS.md`, spec `orchestration/SPEC_ECLAIRAGE.md` v2. Mémoire : `~/.claude/projects/-Users-pasquelin-Applications-webGeometry/memory/` (lire `webgeometry-etat-2026-09-15-lumiere-apres-fix.md`). Lab : port 5174 jamais tué, `public/benchmark-assets` jamais écrit. `export DEVELOPER_DIR=/Library/Developer/CommandLineTools` si git refuse.

## Cinq sessions en parallèle (ListAgents)
Lumière (celle-ci), Geometry, Calculs, Compilateur, Validateur. **Plus aucun push sur origin** : seul le Validateur pousse `develop` après simplify et validation ; les autres fusionnent en local. Préavis mutuel avant toute fusion touchant `sdk-browser`. Verrou de mesure `.claude/mesure.lock` : `mkdir` puis fichier `proprietaire` (« lumiere <lot> date »), `rm proprietaire && rmdir` seulement par le titulaire, message « verrou rendu » aux autres à chaque libération, jamais d'attente en boucle ; un banc lancé sans verrou = faute (arrivé deux fois cette session, briefs à écrire en conséquence).

## Fusionné cette session (develop local)
- `fix/webgpu-lost` → 2dcc8fc (poussé avant l'interdiction) : remplaçant de `BounceGrid` à `BOUNCE_GRID_BYTES`, `bounceLimits.ts`, incidents GPU au banc. **Nouvelle référence d'image WebGPU = 2dcc8fc** (justifié dans JOURNAL.md : develop d'avant plantait, 4390 px contre d3dcd69 imputés aux lots intermédiaires).
- `lot/ombres-lointaines` → b3cf4e9 : simplification 0 px sur 6 séries ; cascades redécoupées à rapport constant 4, ombre du soleil au-delà des cascades contre le proxy (changement d'image voulu, chiffré au journal). 9 → 8 tampons au fragment, groupe de liaison mémorisé.
- `lot/stochastique` → 9318023 : listes de lampes par tuile à deux tranches, boucle exacte des transparents, `directShadowWgsl.ts` ; 0 px sur 10 séries (Emerald 3 vues + classes-materiaux 2 vues, seuils 0 et 1).

## En attente (worktrees en place, à ne pas supprimer)
- `lot/reflet` = 5c5ceaa (worktree `.claude/worktrees/lot-reflet`, base 32c95b9) : reflet pour toute surface réfléchissante (LR7), WebGPU, par propriété de matériau, vue miroir par réflexion de Householder, ciseau à l'emprise, composition Fresnel, `REFLECTION_SETTINGS`, harnais `--reflet on|off` (les réglages moteur passent par un objet unique, `pageEclairage.mjs` a regagné 7 lignes). Portes vertes, fumée sans incident. **Le chemin de dessin n'a jamais tourné** (Emerald : 0 surface réfléchissante). Banc prêt : fixture `packages/asset-compiler-rust/fixtures/classes-materiaux/miroir.gltf` (sol métallique lisse, vitre BLEND ior 1,5, 3 cubes, soleil + ponctuelle), cache compilé `.mesure/cache-miroir`. À jouer sous verrou : `node scripts/mesure/banc.mjs --moteur webgpu --cache-avant/apres .mesure/cache-miroir --vues generale,detail --images 60 --pixelError 0,1 --reflet off` puis `--reflet on`, diff des PNG, `surfacesReflechissantes` > 0, `incidentsGpu` nul ; regarder les images. Restes du lot : plafond en millisecondes (pas en nombre de vues), composition transmissive de l'eau (lit les sondes dans la direction du miroir), WebGL2, rebase sur develop.
- Demande du Validateur (push de develop bloqué tant que non tranché, réponse donnée : lot pris) : **ombre lointaine sur la passe de mélange** — `directLightingWgsl.ts` donne `SUN_FAR_STUB_WGSL` (retourne 1) au chemin blend ; lier le proxy à la passe de mélange, retirer le bouchon, une seule façon d'éclairer opaques et transparents. Premier lot de la reprise, après rebase sur le commit du Validateur (`gpuPeriodicReadback.ts` partagé Hi-Z / ombre lointaine, `grantCapability`).
- `essai/stochastique-accumulation` (74300fe) : base du futur lot « historique en cible de rendu ping-pong ».

## Suite du plan
Ombre lointaine sur la passe blend → preuve du reflet sur le banc miroir puis fusion → reflets flous → ombres colorées des semi-transparents + translucidité des feuilles (propriété de matériau, transmission diffuse) → import : alpha binaire déclaré BLEND → MASK (avec Compilateur ; 1,73 M triangles de feuillage d'Emerald sans ombre) → optimisation mesurée (rebond 1,01 ms, oracle 18,6 % → 10 %, retard 367 → 250 ms, lampes WebGL2 en dur). Trou de test à combler à la livraison : `webgpuBindBudget.test.ts` ne couvre pas `createDeferredLayouts`. `scripts/mesure/pageEclairage.mjs` : 193 lignes, fonction sérialisée par Playwright, injection par objet unique en place. Livraison finale : remesure au calme (charge < 4), tests Sonnet, `npm run validate` complet. Supprimer ce fichier quand tout est livré.

## Première action à la reprise
ListAgents ; lire les messages des voisines ; rebaser `lot/reflet` ; lancer le lot « ombre lointaine sur la passe blend » (Opus) puis le banc miroir (Sonnet, verrou).
