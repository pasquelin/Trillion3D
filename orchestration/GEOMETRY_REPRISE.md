# Reprise — WebGeometry sans Three.js (15 septembre 2026, ~13 h, session mise en pause)

## Rôles, non négociables
- Toi (Fable) : chef d'orchestre. Tu ne codes pas, tu ne lis pas de code, tu ne lis que les rapports courts des agents (≤ 10 lignes). Réponses à l'utilisateur en 5 lignes, simples, un tableau si des chiffres. Images du harnais à conserver et à montrer (SendUserFile).
- Opus 5 : tout le code. Plusieurs Opus en parallèle autorisés sur des périmètres disjoints (règle levée par l'utilisateur le 15 sept.). Sonnet 5 : lecture, rapports. Haiku : jamais.
- Tests écrits à la fin selon ce qui est retenu ; `npm run validate` complet seulement à la livraison finale ; par lot : tsc, check:changed, check:unused, preuve d'image 0 px.
- Spec : orchestration/SPEC_MOTEUR_SANS_THREE.md. Journal : orchestration/JOURNAL.md, entrées « [session sans-threejs] » (les entrées datées « 16 septembre » sont du 15 : à corriger à la relecture finale). Mots interdits : nom du système de géométrie virtualisée d'Epic et nom de son moteur. Lab (render-tech-lab/) jamais modifié. Port 5174 réservé.
- Emerald n'est QU'une scène de test ; l'utilisateur en change bientôt ; rien dans le code ne se cale sur elle. Harnais déjà générique (poses dérivées des bornes, nom de scène déduit du cache, README « mesurer une autre scène »).
- Fidélité avant vitesse : 0 px au harnais `node scripts/mesure/banc.mjs --moteur webgpu|webgl --avant <ref> --apres <ref> --vues generale,sol,rue --images 60 --pixelError 0,1 --max-pages 100000` (+ caméra mobile, + scène synthétique classes-materiaux), témoin A/A, hash identique, trous 0. Verrou `mkdir /Users/pasquelin/Applications/webGeometry/.claude/mesure.lock` avant CHAQUE campagne, `rmdir` après, jamais tenu entre deux, une seule campagne sur la machine (règle commune aux trois sessions depuis que la machine a chauffé). Durées relevées machine chargée = à rejouer au calme à la livraison finale ; les pixels ne dépendent pas de la charge.
- Chaque Opus copie son `.mesure/out` dans `/Users/pasquelin/Applications/webGeometry/.mesure/out/<lot>/` avant de supprimer son worktree. Worktrees d'agents : `git worktree add /Users/pasquelin/Applications/webGeometry/.claude/worktrees/<lot> -b lot/<lot> develop` + `ln -s .../node_modules`. Fusion : `git -C /Users/pasquelin/Applications/webGeometry merge --ff-only <branche>` puis `branch -f main develop`, rien poussé. Jamais de `git stash`. Jamais de boucle d'attente.

## État de develop = main = 38f1b5c (rien poussé sauf par Lumière)
Fusionnés cette session : lot 3b (résidence/transparents tenus/projection), lot A budget en pages distinctes (nouvelle référence acceptée), lot B transparents GPU (compaction à ordre stable ; seconde référence acceptée : seuil 0 exact, 10 M triangles), encodage, Hi-Z (borne minorante ; cause des pixels = ordre de dessin sur profondeurs exactement égales), harnais toute scène, eau/transmission (17b82a3 : caches shared-blend acceptés, passe de transmission, normale retournée vers l'œil). CPU image générale seuil 0 : 33,6 → 7,8 ms fixe, mobile 17,6 → 10,7 ; GPU = goulot : 29 ms seuil 0, 11,5 seuil 1.

## BLOQUANT EN COURS : develop perd l'appareil WebGPU depuis 8d1fe94 (rebond 3 de Lumière)
Bissection : 30f2b33 OK, 0756387 OK, 8d1fe94 WEBGPU_LOST et tous les suivants ; d3dcd69 OK ; WebGL OK. Repro : `node scripts/mesure/banc.mjs --moteur webgpu --apres <dist> --vues generale --images 5 --pixelError 0 --max-pages 100000 --profil off`. Lumière corrige sur `fix/webgpu-lost` et doit envoyer le SHA à la nouvelle session. À la réception : campagne WebGPU courte sur develop (eau a été fusionnée après rebase sur 56c1012 sans re-preuve WebGPU, sa preuve datait de 0a3c601), puis relancer instances.

## Lots en pause (worktrees et branches laissés en place ; relancer chacun avec un Opus neuf pointé sur son worktree : les identifiants d'agents ne survivent pas à la session)
1. `lot/instances-gpu`, worktree `.claude/worktrees/lot-instances` (H1 : une géométrie, N matrices, racines du DAG partagées) : 7 commits, tête 3579f20, sur 38f1b5c (journal complet, §5 bis = bissection), tsc vert. Preuve WebGPU faite sur d3dcd69 (0 px 6/6 à 1 et 9 instances, mobile, synthétique à 1 ; géométrie 420,6 → 209,1 Mo à 1, 2 652,8 → 209,1 Mo à 9), WebGL sur base courante. Reste : après le correctif WebGPU, rebase, campagnes WebGPU sous verrou (1, 9, mobile, synthétique à 1), fusion ff, images dans `.mesure/out/lot-instances`. Colonne « tas JS » abandonnée volontairement. Synthétique inutilisable à 9 instances (boîtes coplanaires) : espacer la fixture, hors lot.
2. `lot/4c-webgl2`, worktree `.claude/worktrees/lot-4c` : 3 commits (+ wip) : port 4c, triangles sommés à la retenue, tests/oracles ; 0 px 6 séries caméra fixe ; sélection générale seuil 0 5,5 → 4,5 ms. Reste : tableaux typés boîte/bande/triangles/niveau (−14 % au banc, prototypé), caméra mobile 0 px, rebase, fusion ff, supprimer `sauvegarde/lot-4c`, images dans `.mesure/out/lot-4c`. Émission de plages exclue (interface publique lue par le Lab). Non concerné par le bug WebGPU.

## Décision utilisateur en attente
Surfaces à profondeur exactement égale (coplanaires v2 : 57-77 % des égalités intra-cluster, hors format, rien fusionné) : les seuls pixels bloquant historique d'occlusion en mouvement (−3 ms CPU, branche d'essai perdue avec le worktree, code décrit au journal) et partition Hi-Z temporelle (−27 % GPU) opposent des clusters distincts dont le vainqueur est décidé par l'image précédente. Accepter un déplacement de référence de 0,007 % (67/173 px générale, 1 sol, 39 rue) avec vainqueur fixé à la compilation (une couche par cluster) ouvre les deux leviers. Recommandation : accepter. Réglages à généraliser : DEPTH_LAYER_BIAS_UNITS = 16 (sdk-core/depthLayer.ts), plafonds CoplanarBounds::default().

## Suite prévue
H4 relecture GPU jamais bloquante (mesurer d'abord, largement fait) ; lot visibilité WebGPU CPU fixe 7,8 → ~4 ms (D3 scan préfixe parallèle prouvé par oracle `bench/oracles/gpuDrawPrefixOracle.ts`, passes persistantes, listes de pages en delta) ; rembourrage 64 entrées par primitive transparente ; témoin Three.js avec lampes du contrat (vrai chiffre de fidélité eau) ; phase 3 première image (chantier/premiere-image-v3, 22 conflits) ; phase 2 sans Three dans l'hôte. Tableau avant/après par étape à tenir à jour pour l'utilisateur (14 sept. : 33,6 ms ; aujourd'hui 7,8).

## Sessions voisines (ListAgents ; adresses uds:/tmp/cc-socks/*.sock)
- « Lumière » (éclairage : rebond, ombres, lampes, transparents sans ambiance ; corrige fix/webgpu-lost). Règle : prévenir avant toute fusion touchant sdk-browser ; le second à fusionner rebase et rejoue sa preuve. Ne pas toucher : ses fichiers d'éclairage/ombres/sondes/proxy.
- « Calculs mathématiques » (H2 décodage worker+wasm fusionné 30f2b33 ; a confié H1 et H4 ; D3 à ranger dans visibilité).
- Cache Emerald du Lab à régénérer par l'utilisateur (404 lights.json) : `npm run prepare:models`.

## Mémoire (déjà à jour)
~/.claude/projects/-Users-pasquelin-Applications-webGeometry/memory/ (index MEMORY.md) : directive-optimiser-sans-perte, opus-paralleles-autorises, emerald-scene-de-test-pas-le-moteur, images-harnais-a-conserver, decision-budget-pages-distinctes, webgeometry-etat-2026-09-16-nuit (= état de la session), webgeometry-lot-transparents-gpu.
