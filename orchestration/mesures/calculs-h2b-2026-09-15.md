# Calculs, lot H2b : décodeur de pages, JavaScript contre WebAssembly (2026-09-15)

Machine : darwin/arm64, Node v26.8.2. Commit `41f7c4784639e476abf07c4a1ce1701ab076aea1`.
Médiane sur N tours après échauffement ; « Retenu » exige l'égalité bit à bit ET un gain.
Machine chargée pendant la mesure (charge moyenne sur une minute : 29.2, seuil 4) : temps non concluants, seule la colonne « Identique » fait foi.
Le module recopie la page dans sa mémoire linéaire avant de lire son en-tête : sur une page refusée dès l'en-tête, cette copie coûte plus que le refus, et la ligne H2b2 n'est donc pas retenue. La rendre rapide demanderait de redire les bornes de l'en-tête en JavaScript, c'est-à-dire de tenir deux vérités au lieu d'une ; le refus reste identique, avec la même cause.
DPR, résolution d'affichage et FPS : sans objet ici, ces mesures sont des calculs CPU purs.

| Calcul | Fichier | Avant (ms) | Après (ms) | Gain | Identique | Retenu |
|---|---|---|---|---|---|---|
| H2b1 décodage de page, wasm contre JS | `packages/sdk-browser/geometryPageWasm.ts` | 11.777 | 8.279 | 29.7 % | oui | oui |
| H2b2 refus de page, wasm contre JS | `packages/sdk-browser/geometryPageWasm.ts` | 0.034 | 0.048 | -41.2 % | oui | non |

2 calculs comparés, 1 retenus, 0 écarts bit à bit.
