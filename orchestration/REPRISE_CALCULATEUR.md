# Calculateur — handoff

Confirm title `Calculateur` with `get_session("self")`. Follow `AGENTS.md`; plan: `SPEC_MOTEUR_SANS_THREE.md`, R1a–R1f. Nothing pushed by this session; only the Validateur pushes.

## State on 2026-09-16, 16:00

- **Delivered and merged** (develop 54c7fb45, pushed up to 88af5cde): the computations of `sdk-browser` no longer use Three.js — foundation `sdk-core/math*.ts` (M1 matrices/vectors/colours, M2 volumes, M3a hierarchy + camera), per-frame WebGPU path on `EngineCamera` (`cameraWorld.ts`, held by `frameGateCore.ts`: one view-projection, zero allocation per frame), loading/explorer (`hostWorldMatrices.ts`, `hostWorldBounds.ts`), diagnostics, `visibilityShadingNormal.ts`. `three` importers 137 → 98, all witnesses, host boundaries, rigs or host resources, locked by `test/engineNoThree.test.mjs` and `engineNoThreeMath.test.mjs`. Audit corrections merged: `setWebgpuTransform` resolves ancestors before inversion (`SINGULAR_PARENT_TRANSFORM`), depth convention conditional (`depthConvention.ts`, `DepthCamera`), normal proof criterion oriented with unit norm.
- **Ready to merge, one SHA, every gate green**: `calculs/final-2` **57c82135** (develop 3af8fcec merged, merge-tree clean). Contains: audit reproductions as browser tests (parent-stale `setTransform` in the real backend, depth convention full engine with the mutation killed at 48 px, normal substitutions 365/365 refused), bit-identical perf lot (`frustumPlanesFromMatrix` −47 %, `visibilityShadingNormal` +9 %, no allocation per shaded pixel), honest harness counters (`triangles` nullable, `imageTenue`, `repliSelectionGpu`, Hi-Z counters under contract names, harness tests in `npm test`), synchronous `drawnTriangles` with coverage `selected − drawn − uncovered` in `resume.md`, three dead re-exports removed. Proof on 57c82135: `validate` green end to end, `npm test` 1292, `test:gpu` 26/26, campaign 068b61ca → 57c82135 WebGPU emerald-square 3 views × 2 thresholds moving camera 60 frames: 0/921 600 px on 6 series, coverage 0, no held frame, no GPU fallback. Measurements outside the repo under `.mesure/out/calculs/` (`final-2/`, `agents-2026-09-16/`).
- Decisions kept: the per-frame API takes `EngineCamera` and refuses a raw host camera (converting there would put an inverse and six planes back per frame and hide an unresolved rig); `addInstance(transform: THREE.Matrix4)`, `PageRec.matrix`, `explorer.bounds/center` stay host-written objects (R8), read once into flat buffers, never computed with.

## To do, in order

1. **Merge `calculs/final-2` (57c82135) into develop and push** — Validateur (session vanished on 2026-09-16 afternoon; if none, the user decides who merges). After the merge delete `calculs/final-2`.
2. **Controlled speed campaign** — the only missing verdict. Every bench of 2026-09-16 ran under load 12–207: pixel proofs stand, speed numbers are observations. Needs load < 6, other sessions stopped, order documented, repetitions kept, A/A variance, residency controlled: `node --expose-gc --experimental-strip-types packages/sdk-browser/bench/{socle-math,volumes,hierarchie,normale}.bench.mjs` then `scripts/mesure/banc.mjs --moteur webgpu --avant 35db01db --apres <develop> --vues generale,sol,rue --images 60 --pixelError 0,1 --camera-mobile`. Loaded observations: M3a 14/14 lines faster (+12 % to +82 %), M2 15/16 (+37 % to +87 % on boxes and spheres), M1 16/24; `determinantMatrix4` and `composeMatrix4` +4 to +9 % slower than Three, stable over three A/B passes, cause unknown (neither read order nor container); batch deficits of `normalMatrix3`/`multiplyMatrix4` attributed to the bench storing one `Float64Array` per element while the engine stores one buffer — not re-verified by changing the bench.
3. **Singular transforms and the normal fallback** — open, independent of the migration (audit on 694da77f, confirmed). Counter-example: local triangle `(0,0,0),(1,0,0),(0,1,0)` under scale `(1,1,0)` then a 90° rotation about Y keeps area 0.5 and geometric normal `+X`; the GPU fallback (`inverseTransposeWgsl.ts`, `select(v, …, regulier)`) returns the LOCAL normal `+Z` (lighting ≈ 0.09 instead of ≈ 0.8 per channel), the CPU (`mathMatrix3.normalMatrix3`, zero on `det === 0`, as Three) returns zero. Settle by convention: a face keeping a non-zero area gets a world normal (cross product of transformed edges), a collapsed face is a separate case, an unsupported transform is refused explicitly. Same decision covers the guard rule (absolute threshold vs normalised determinant) and the 31 % disagreement between `matrixWindingCw` (3×3) and `determinantMatrix4` (4×4) on constructed singular matrices.
4. **Two red benches on develop, not from these lots**: `packages/sdk-browser/bench/residence.bench.mjs` broken since af8a53ad (`gpuDagRuntime.ts:38`, `updateResidencyFlags` signature, bench not updated — Geometry); `packages/sdk-core/bench/g-soleil-g.bench.mjs` G8 gives NaN in the sun cascade bounds since 061059e0 (Lumière is checking whether the bench or the code is wrong). `npm run bench:calculs` was not run whole: other red benches may exist.
5. **Two `?? 0` left in the SDK**: `explorerMetrics.ts:98` (`transparentSubmittedTriangles ?? 0` inside a sum) and `explorerRender.ts:114` (`drawCalls`). Make them `null` or prove the counter is always measured.
6. **Mip seam**: defect 7 fixed magnification only; lifting the limit above `wrapAxis` means a gutter of replicated border texels per level laid by the atlas compiler, which would delete `wrapUv`, `WrapTaps` and the four taps.
7. Then M5 (worker/Wasm only where a calm measurement shows a cost), R7/R8 (WebGL2 renderer, host API without Three types).

## Workflow

- Opus: code, reproductions, benches; Sonnet: tests, campaigns, read-only audits. Worktree from `develop`; never stash or push. A fresh worktree has no `node_modules`: symlink the repo's. `LAB_ROOT=/Users/pasquelin/Applications/render-tech-lab` for `test:gpu` and `scripts/mesure`; never port 5174.
- One proof per lot on its exact SHA: bit-identical benches, `test:gpu`, common-bench campaign with A/A witness. Equivalence to the previous code and geometric correctness are different claims: name which one each test makes.
- Bug fixes: reproduce on CPU **and executed GPU** before fixing; every correctness test killed by mutation; replay a supplied counterexample before concluding.
- Durations compare only at equal input, camera, resolution, DPR, threshold, budget **and residency** — and load. Otherwise they are observations, not verdicts.

## Traps this project has already sprung

- **A guard on a raw determinant judges scale, not degeneracy** (`±s³`): `abs(det) < 1e-20` dropped out at `(1e-20)^(1/3)`. Normalised guard, one writing, `inverseTransposeWgsl.ts`.
- **An unsigned angle accepts −N and 0**: `|a·b|` and `atan2(0,0) = 0` let a fully wrong shader pass 365 cases (fixed 2026-09-16: signed dot, NaN on null, unit norm, substitution proven on the real shader).
- **A zero can be a fallback, not a measurement**: `triangles = 0` came from an idle Three renderer's `info.render.triangles`, not from a held frame. Publish `null`, or the counter that is actually kept on that engine.
- **The parent's world matrix must be resolved before inverting it**: `setTransform` used a stale parent (x = 3 requested, 13 obtained). A `sameElements` early return needs the same resolution.
- **A plausible thesis is not a defect** (lot 6 cone/winding); **a witness can miscount both ways** (560 → 54 hid 119 false positives); **pixels called noise had a cause** (defect 7 found by bisection); **a test matching shader text proves nothing**; an interrupted agent once left a verification mutation in the source — check the diff before believing a failure.
