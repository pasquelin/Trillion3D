# Calculateur — todo

Confirm title `Calculateur` with `get_session("self")`. Follow `AGENTS.md`. Never push (Validateur only). Worktree created by hand from `develop` (`git worktree add -b <branch> .claude/worktrees/<name> develop`), symlink the repo's `node_modules`, `LAB_ROOT=/Users/pasquelin/Applications/render-tech-lab` exported for `test:gpu` and `scripts/mesure/banc.mjs`, never port 5174. Every lot: Opus codes, chef runs `npm test`, `npm run test:gpu` and `banc.mjs --moteur webgpu --avant develop --apres <branch> --vues generale,rue --images 30 --pixelError 0,1 --out .mesure/out/<name>` at delivery, then merges `--no-ff` into develop and deletes branch and worktree at once. Image reference = develop ≥ e8819543. Known red unit tests, not ours: six need a built `dist/` (red in a worktree only); six `webgpuPages` are red on develop since Geometry's raster merge b72278c6 (Geometry keeps them for the user's verdict). `webgpuPages.12` is flaky under load. Measured numbers on this Mac are sanity checks, never engine constants.

1. **On the user's go**: the WebGL2 renderer of our own, then the host API without Three types. `PageRec.matrix` and `ClusterRoot.world` are now matrices the ENGINE fills (`hostWorldPlacements.ts`): flattening them to `Float64Array` asks the host for nothing, only its readers — `addInstance`/`updateInstance` and the witness backends — still take a `THREE.Matrix4`. Some thirty files read `.elements` on them.

Unmeasured, to measure when convenient: gain of shared-memory pages (`--isolation on` vs off) and of the Wasm path (`--chemin-math js` vs `wasm`); `normalMatrix3` per fragment in `visibilityShadingNormal.ts`; `gpuLightTilesShader` light lists on open sky.

Done and on develop, for orientation only: shared memory for page workers (`pageDecodeShared*.ts`, `explorerCapabilities.pageDecode`), arena rebuilt by generation and whole hierarchy in `math_hierarchy.rs`, engine-composed chain and subtree (`hostWorldChain.ts`, `hostWorldTree.ts`), pages and cluster roots carrying the engine's own world matrices with no `updateMatrixWorld` left on the frame path (`hostWorldPlacements.ts`, lot 8), camera-relative rendering, reversed-Z with infinite far plane (`depthConvention.ts`), screen-error switch (`screenError`). All 0 px against develop.
