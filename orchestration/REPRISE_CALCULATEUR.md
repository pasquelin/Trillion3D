# Calculateur — todo

Confirm title `Calculateur` with `get_session("self")`. Follow `AGENTS.md`. Never push (Validateur only). Worktree from `develop`, symlink the repo's `node_modules`, `LAB_ROOT=/Users/pasquelin/Applications/render-tech-lab`, never port 5174. Every lot: Opus codes, Sonnet tests, then bit-identical benches, `test:gpu`, common-bench campaign against develop ≥ 37a55d59 with `--pixelError 0,1` and A/A witness (depth ties at LOD seams are accepted at threshold 1). Measured numbers on this Mac are sanity checks, never engine constants.

1. **Speed levers** — unify the buffers of every caller of `multiplyMatrix4` in `sdk-browser` (today `Float64Array`/`Float32Array`/`Array` mixed; +38 % measured at a monomorphic site); one rule for "singular" on CPU and GPU (CPU `det === 0` in f64, GPU normalised determinant in f32 in `inverseTransposeWgsl.ts`). Bits unchanged.
2. **Camera-relative rendering** — subtract the camera position in double before the f32 upload (`cameraWorld.ts`, `EngineCamera.view`), page and instance world matrices camera-relative once per frame. 0 px on Emerald; proof on a scene offset by 50 km (no jitter).
3. **Reversed-Z, infinite far plane** — `mathCamera.ts` projection reversed (1 near → 0 far, far infinite), `depthCompare: 'greater'`, clear to 0, every depth reader aligned: Hi-Z pyramid min/max, `hizCorners`, `hizDepth`, `visibilityRaster`, `visibilityProjection`, shaders, shadow maps, transmission; both conventions kept coherent through `depthConvention.ts`. Image changes at LOD seams: user re-baselines the reference; campaign at both thresholds, A/A 0, far-scene test without flicker.
4. **Shared memory for the page workers** (`pageDecodePool.ts`, `pageDecodeTask.ts`, `pageDecodeHost.ts`) — `SharedArrayBuffer` read directly by the main thread. Mandatory: one writer per zone (each worker owns a region and a slot); page state through `Atomics` (free → decoding → ready → consumed), read only when `ready`, no polling loop; gate `crossOriginIsolated && SharedArrayBuffer`, else the current transfer path, announced in `explorerCapabilities`; COOP/COEP headers only on `scripts/mesure/serveur.mjs --isolation on|off` (default off), the Lab untouched. Proof: pages bit-identical between both paths, 0 px on both, A/A, stress with more pages than slots, worker killed mid-page releases its slot, fallback taken when the gate is false.
5. **Rust/Wasm follow-up** (POC merged 0f95e425: `math.rs` in `page-codec-wasm`, `wasmArena.ts`, `mathPathGovernor.ts`, no measured gain yet) — arena robust to memory growth (views rebuilt by generation) so engine buffers live in it without copies; whole hierarchy update in Rust (sequential parent → child); `mathBatch` and `mathPath` exposed by the harness so the governor's choice is visible. Same rails and proofs as 4.
6. **Screen-error metric option** (experiment `calculs/exp-erreur-ecran`, d8726485, not merged) — at threshold 1 the reference formula draws 7–17 % fewer triangles and changes 0.6–5.7 % of pixels near the near plane and off-axis; ours is certified. Keep ours; merge the switch only if the user wants the lighter mode.
7. **Then** the WebGL2 renderer of our own and the host API without Three types.
