# Calculateur — todo

Confirm title `Calculateur` with `get_session("self")`. Follow `AGENTS.md`. Never push (Validateur only). Worktree created by hand from `develop` (`git worktree add -b <branch> .claude/worktrees/<name> develop`), symlink the repo's `node_modules`, `LAB_ROOT=/Users/pasquelin/Applications/render-tech-lab` exported for `test:gpu` and `scripts/mesure/banc.mjs` (they load playwright and vite from the Lab), never port 5174. Every lot: Opus codes, chef runs tests at delivery (`npm test`, `npm run test:gpu`, campaign `banc.mjs --moteur webgpu --avant develop --apres <branch> --vues generale,rue --images 30 --pixelError 0,1 --out .mesure/out/<name>` from the main checkout), then merge `--no-ff` into develop, delete branch and worktree at once. Image reference = develop ≥ e8819543 (reversed-Z accepted by the user on 2026-09-16; `.mesure/out/calc-3-reference`). Six unit tests need a built `dist/` (provenance, cluster-metrics, public, replicate-instances, resource-validation, declarations): red in a worktree, green in the main checkout. `webgpuPages.12` "mixed GPU and transparent pages wait for initial coverage" is flaky under machine load; rerun alone before blaming code. Measured numbers on this Mac are sanity checks, never engine constants.

1. **Finish the engine hierarchy**: `pageSelectionCollect.ts` still lends live `THREE.Matrix4` (`PageRec.matrix`, `ClusterRoot.world`) to the WebGPU runtime and `frameGateCore.updateWorlds` refreshes them with `source.updateMatrixWorld(true)`; make both read the engine index (`hostWorldTree.ts`), then drop `resolveHostSubtree` from `sceneMeshes.ts` and the `updateWorldMatrix` at `webgpuPagesTransform.ts`. Touches `gpuDag*`/`webgpuPages*`: coordinate with Geometry (lots coupe-sans-relecture, transparents-un-appel) first.
2. **Then** the WebGL2 renderer of our own and the host API without Three types.

Done and on develop, for orientation only: engine-composed chain and subtree (`hostWorldChain.ts`, `hostWorldTree.ts`, lights/bounds/framing, JS and Wasm paths in the arena, bit-identical to Three, 0 px); six `webgpuPages` unit tests red on develop itself since Geometry's raster merge b72278c6, not ours; Rust/Wasm follow-up (arena views rebuilt by generation, whole hierarchy in `math_hierarchy.rs` bit-identical to JS, harness `--chemin-math auto|js|wasm` and table « Chemin de calcul en lot »; 0 px on the wasm path, js/wasm gain still unmeasured; `createHierarchyLot` has no engine caller yet, only benches; `cargo test` in `page-codec-wasm` has one stale expectation `en_tete_trop_court_puis_magie_puis_bornes`, red before the lot, outside `npm run validate`), shared memory for page workers (447147c6: `pageDecodeShared*.ts`, one worker = one slot = one region, `Atomics` states, gate announced as `explorerCapabilities.pageDecode`, harness `--isolation on|off`; 0 px and A/A 0 px with isolation on, gain not measured), speed levers (Float64Array buffers at every `multiplyMatrix4` caller, single singular rule `mathSingular.ts` shared with `inverseTransposeWgsl.ts`), camera-relative rendering (`mathRenderOrigin.ts`, `cameraRenderOrigin.ts`, worlds recentred at upload, 0 px), reversed-Z with infinite far plane (`depthConvention.ts` single source: `DEPTH_COMPARE = 'greater'`, clear 0, Hi-Z level 0 cleared to `DEPTH_CLEAR`; host `far` kept as a frustum plane by `frustumFarPlane`), screen-error metric switch (`screenError`, default `'certifiee'`, `'reference'` = lighter mode that changes pixels). Two things to measure when convenient: `normalMatrix3` normalises once more per call and runs per fragment in `visibilityShadingNormal.ts` (cache per page if it shows); `gpuLightTilesShader` takes the whole world for a tile without opaque (no far plane to unproject), cost of light lists on open sky.
