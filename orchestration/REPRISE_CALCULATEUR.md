# Calculateur — todo

Confirm title `Calculateur` with `get_session("self")`. Follow `AGENTS.md`. Never push (Validateur only). Worktree created by hand from `develop` (`git worktree add -b <branch> .claude/worktrees/<name> develop`), symlink the repo's `node_modules`, `LAB_ROOT=/Users/pasquelin/Applications/render-tech-lab` exported for `test:gpu` and `scripts/mesure/banc.mjs` (they load playwright and vite from the Lab), never port 5174. Every lot: Opus codes, chef runs tests at delivery (`npm test`, `npm run test:gpu`, campaign `banc.mjs --moteur webgpu --avant develop --apres <branch> --vues generale,rue --images 30 --pixelError 0,1 --out .mesure/out/<name>` from the main checkout), then merge `--no-ff` into develop, delete branch and worktree at once. Six unit tests need a built `dist/` (provenance, cluster-metrics, public, replicate-instances, resource-validation, declarations): red in a worktree, green in the main checkout. `webgpuPages.12` "mixed GPU and transparent pages wait for initial coverage" is flaky under machine load; rerun alone before blaming code. Measured numbers on this Mac are sanity checks, never engine constants.

1. **Reversed-Z, infinite far plane — user decision pending.** Branch `lot/calc-3-reversed-z`, worktree `.claude/worktrees/lot-calc-3-reversed-z`, head `c5abe3bd`, develop merged in. Unit tests and `test:gpu` green (27/27). Campaign in `.mesure/out/calc-3-reversed-z-v2`: same cut hash and triangles both sides, A/A 0 px, difference generale 5059 px / rue 1900 px at threshold 0 (4752 / 1220 at threshold 1). Attributed by witnesses: not the coplanar bias, not shadows, not small triangles; develop at near 0.1 has thousands of exact depth ties (stacked frames, roof edges) resolved by draw order; with near 2 on both sides the gap falls to 58 px and develop's own image moves by 5034 px while the lot's moves by 1 px. Verdict of the lot: fidelity gain. If the user accepts: merge, delete worktree and branch, rerun the campaign develop-before vs develop-after at both thresholds and record it as the new image reference (like 37a55d59). If refused: delete branch and worktree, keep `depthConvention.ts` as the single source (already the case on develop for the direct convention).
2. **Shared memory for the page workers** (`pageDecodePool.ts`, `pageDecodeTask.ts`, `pageDecodeHost.ts`) — `SharedArrayBuffer` read directly by the main thread. Mandatory: one writer per zone (each worker owns a region and a slot); page state through `Atomics` (free → decoding → ready → consumed), read only when `ready`, no polling loop; gate `crossOriginIsolated && SharedArrayBuffer`, else the current transfer path, announced in `explorerCapabilities`; COOP/COEP headers only on `scripts/mesure/serveur.mjs --isolation on|off` (default off), the Lab untouched. Proof: pages bit-identical between both paths, 0 px on both, A/A, stress with more pages than slots, worker killed mid-page releases its slot, fallback taken when the gate is false. Gain not measured yet: today's path transfers (no copy, one hop); expect fewer allocations and no message hop, measure at delivery.
3. **Rust/Wasm follow-up** (POC merged 0f95e425: `math.rs` in `page-codec-wasm`, `wasmArena.ts`, `mathPathGovernor.ts`, no measured gain yet) — arena robust to memory growth (views rebuilt by generation) so engine buffers live in it without copies; whole hierarchy update in Rust (sequential parent → child); `mathBatch` and `mathPath` exposed by the harness so the governor's choice is visible. Same rails and proofs as 2.
4. **Then** the WebGL2 renderer of our own and the host API without Three types.

Done and on develop, for orientation only: speed levers (Float64Array buffers at every `multiplyMatrix4` caller, single singular rule `mathSingular.ts` shared with `inverseTransposeWgsl.ts`), camera-relative rendering (`mathRenderOrigin.ts`, `cameraRenderOrigin.ts`, worlds recentred at upload, 0 px), screen-error metric switch (`screenError`, default `'certifiee'`, `'reference'` = lighter mode that changes pixels). `normalMatrix3` now normalises once more per call and is called per fragment in `visibilityShadingNormal.ts`: measure, cache per page if it shows.
